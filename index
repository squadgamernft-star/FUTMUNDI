<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FUTMUNDI</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <script src="https://unpkg.com/@tonconnect/ui@latest/dist/tonconnect-ui.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Rajdhani:wght@500;600;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        :root {
            --cyan: #00d4ff; --green: #00ff88; --orange: #ff8c00;
            --red: #ff4444; --gold: #ffd700; --purple: #a855f7;
            --grass: #2d7a1f; --grass2: #3aaa28;
            --surface: rgba(255,255,255,0.04); --border: rgba(0,212,255,0.2);
        }

        /* ─── WORLD FLAGS BACKGROUND ─── */
        body {
            font-family: 'Rajdhani', sans-serif;
            background: #060f06;
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
            position: relative;
        }

        /* Animated world flags background */
        .flags-bg {
            position: fixed;
            inset: 0;
            z-index: 0;
            overflow: hidden;
            pointer-events: none;
        }
        .flags-bg::before {
            content: '';
            position: absolute;
            inset: 0;
            background:
                radial-gradient(ellipse at 20% 20%, rgba(0,150,60,0.18) 0%, transparent 55%),
                radial-gradient(ellipse at 80% 80%, rgba(0,80,200,0.14) 0%, transparent 55%),
                radial-gradient(ellipse at 50% 50%, rgba(220,20,60,0.08) 0%, transparent 60%),
                linear-gradient(160deg, #061206 0%, #0a1a0a 40%, #060f06 100%);
        }
        .flag-stripe {
            position: absolute;
            opacity: 0.06;
            animation: floatFlag linear infinite;
        }
        @keyframes floatFlag {
            0%   { transform: translateY(110vh) rotate(-8deg); opacity: 0; }
            10%  { opacity: 0.07; }
            90%  { opacity: 0.07; }
            100% { transform: translateY(-110vh) rotate(-8deg); opacity: 0; }
        }
        .flag-emoji {
            position: absolute;
            font-size: 2.2em;
            opacity: 0;
            animation: floatEmoji linear infinite;
            filter: grayscale(0.4) opacity(0.5);
        }
        @keyframes floatEmoji {
            0%   { transform: translateY(105vh) rotate(-15deg) scale(0.8); opacity: 0; }
            10%  { opacity: 0.18; }
            90%  { opacity: 0.18; }
            100% { transform: translateY(-105vh) rotate(10deg) scale(1.2); opacity: 0; }
        }
        .ball-bg {
            position: absolute;
            font-size: 1.6em;
            opacity: 0;
            animation: floatBall ease-in-out infinite;
        }
        @keyframes floatBall {
            0%   { transform: translateY(108vh) rotate(0deg); opacity:0; }
            10%  { opacity:0.1; }
            90%  { opacity:0.1; }
            100% { transform: translateY(-108vh) rotate(360deg); opacity:0; }
        }
        .content-wrap { position: relative; z-index: 1; display: flex; flex-direction: column; min-height: 100vh; }

        /* ─── LANGUAGE SELECTOR ─── */
        .lang-bar {
            background: rgba(0,0,0,0.7);
            border-bottom: 1px solid rgba(0,212,255,0.1);
            padding: 6px 18px;
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
            justify-content: flex-end;
        }
        .lang-bar span { color: #888; font-size: 0.72em; text-transform: uppercase; letter-spacing: 1px; margin-right: 4px; }
        .lang-btn {
            background: rgba(255,255,255,0.05);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 20px;
            color: #aaa;
            font-size: 0.72em;
            padding: 3px 9px;
            cursor: pointer;
            transition: all 0.2s;
            font-family: 'Rajdhani', sans-serif;
            font-weight: 600;
        }
        .lang-btn.active { background: rgba(0,212,255,0.15); border-color: var(--cyan); color: var(--cyan); }
        .lang-btn:hover:not(.active) { border-color: rgba(255,255,255,0.3); color: #fff; }

        /* ─── TOP BAR ─── */
        .top-bar { padding: 12px 25px; display: flex; justify-content: space-between; align-items: center; background: rgba(0,0,0,0.65); border-bottom: 1px solid rgba(45,122,31,0.35); backdrop-filter: blur(14px); flex-wrap: wrap; gap: 12px; position: sticky; top: 0; z-index: 100; }
        .stats-container { display: flex; gap: 18px; flex-wrap: wrap; color: #888; font-size: 0.8em; text-transform: uppercase; letter-spacing: 0.8px; }
        .stats-container span { display: flex; align-items: center; gap: 5px; }
        .stats-container strong { color: var(--green); font-weight: 700; font-size: 1.1em; text-shadow: 0 0 8px rgba(0,255,136,0.4); }
        #ton-connect-button { z-index: 1000; }

        /* ─── MAIN ─── */
        .main-content { flex: 1; display: flex; flex-direction: column; align-items: center; padding: 25px 20px 40px; width: 100%; max-width: 1100px; margin: 0 auto; }
        .title-container { text-align: center; margin-bottom: 35px; }
        h1 { font-family: 'Bebas Neue', sans-serif; font-size: 3.2em; font-weight: 400; background: linear-gradient(90deg, var(--green), #7fff00, var(--gold)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-transform: uppercase; letter-spacing: 8px; line-height: 1; }
        .title-sub { font-size: 0.82em; color: #888; letter-spacing: 3px; text-transform: uppercase; margin-top: 4px; font-family: 'Rajdhani', sans-serif; }

        /* ─── LAYOUT ─── */
        .layout-grid { display: flex; width: 100%; justify-content: center; align-items: center; gap: 50px; margin-bottom: 35px; }
        .car-container { position: relative; width: 100%; max-width: 460px; display: flex; flex-direction: column; align-items: center; gap: 18px; }
        .car-base-glow { position: absolute; bottom: 65px; width: 75%; height: 18px; background: var(--green); filter: blur(35px); opacity: 0.35; z-index: 1; }
        .car-image { width: 100%; border-radius: 14px; border: 2px solid rgba(45,122,31,0.7); box-shadow: 0 0 35px rgba(45,122,31,0.4); background: #000; display: block; transition: transform 0.4s cubic-bezier(0.175,0.885,0.32,1.275); z-index: 2; min-height: 180px; object-fit: cover; cursor: pointer; }
        .car-image:hover { transform: scale(1.03) translateY(-5px); }
        .menu-column { display: flex; flex-direction: column; gap: 22px; min-width: 200px; }
        .word-btn { background: transparent; color: #ccc; border: none; padding: 10px 12px; font-size: 1.05em; font-weight: 600; font-family: 'Rajdhani', sans-serif; cursor: pointer; position: relative; text-align: center; transition: all 0.3s ease; text-transform: capitalize; letter-spacing: 1px; outline: none; }
        .word-btn::after { content: ''; position: absolute; bottom: 0; left: 50%; transform: translateX(-50%); width: 0; height: 2px; background: linear-gradient(90deg, transparent, var(--green), transparent); transition: width 0.35s ease, opacity 0.35s ease; opacity: 0; }
        .word-btn:hover { color: #fff; text-shadow: 0 0 10px rgba(255,255,255,0.7), 0 0 22px rgba(0,255,136,0.5); transform: translateY(-2px); }
        .word-btn:hover::after { width: 80%; opacity: 1; }
        .primary-btn { background: linear-gradient(135deg, rgba(45,122,31,0.18), rgba(0,200,80,0.12)); border: 1px solid rgba(45,122,31,0.7); color: var(--green); padding: 14px 28px; border-radius: 10px; font-weight: 700; font-size: 1em; font-family: 'Orbitron', sans-serif; cursor: pointer; transition: all 0.3s ease; text-transform: uppercase; letter-spacing: 2px; width: 100%; position: relative; overflow: hidden; z-index: 3; }
        .primary-btn::before { content: ''; position: absolute; top: 0; left: -100%; width: 50%; height: 100%; background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent); transform: skewX(-20deg); transition: 0.5s; }
        .primary-btn:hover::before { left: 150%; }
        .primary-btn:hover { background: linear-gradient(135deg, rgba(45,122,31,0.35), rgba(0,200,80,0.25)); box-shadow: 0 0 25px rgba(0,255,136,0.4); color: #fff; }
        .bottom-menu { display: flex; justify-content: center; gap: 40px; margin-top: 10px; }
        #status-msg { margin-top: 18px; font-size: 1em; color: #888; min-height: 24px; text-align: center; padding: 8px; font-weight: 600; }

        /* ─── SOCCER BALLS (fuel) ─── */
        .fuel-cartridges { display: flex; gap: 10px; justify-content: center; margin-top: 4px; }
        .fuel-cartridge {
            width: 36px; height: 36px; border-radius: 50%;
            border: 2px solid rgba(45,122,31,0.4);
            background: rgba(45,122,31,0.1);
            cursor: pointer; transition: all 0.3s;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.4em; position: relative; overflow: hidden;
        }
        .fuel-cartridge.full {
            background: radial-gradient(circle at 35% 35%, #fff 0%, #444 60%, #000 100%);
            border-color: var(--green); box-shadow: 0 0 10px rgba(0,255,136,0.5);
            animation: ballGlow 2s ease-in-out infinite;
        }
        @keyframes ballGlow { 0%,100%{box-shadow:0 0 8px rgba(0,255,136,0.4)} 50%{box-shadow:0 0 18px rgba(0,255,136,0.8)} }
        .fuel-cartridge.empty {
            background: rgba(255,255,255,0.03);
            border-color: rgba(255,255,255,0.1);
            filter: grayscale(1) opacity(0.35);
        }
        .fuel-cartridge:hover { transform: scale(1.15) rotate(10deg); }
        .fuel-label { font-size: 0.72em; color: #888; text-align: center; letter-spacing: 1px; text-transform: uppercase; }

        /* ─── MODAL ─── */
        .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.88); backdrop-filter: blur(8px); z-index: 999; justify-content: center; align-items: center; padding: 20px; }
        .modal-overlay.active { display: flex; }
        .modal { background: linear-gradient(145deg, #0a130a 0%, #060d06 100%); border: 1px solid rgba(45,122,31,0.3); border-radius: 18px; max-width: 750px; width: 100%; max-height: 88vh; overflow-y: auto; box-shadow: 0 0 60px rgba(0,255,136,0.1); animation: modalIn 0.35s cubic-bezier(0.175,0.885,0.32,1.275); }
        @keyframes modalIn { from { opacity: 0; transform: scale(0.85) translateY(30px); } to { opacity: 1; transform: scale(1) translateY(0); } }
        .modal-header { padding: 18px 22px 14px; border-bottom: 1px solid rgba(45,122,31,0.25); display: flex; justify-content: space-between; align-items: center; position: sticky; top: 0; background: #0a130a; border-radius: 18px 18px 0 0; z-index: 2; }
        .modal-title { font-family: 'Bebas Neue', sans-serif; font-size: 1.3em; font-weight: 400; color: var(--green); letter-spacing: 3px; text-transform: uppercase; }
        .modal-close { background: none; border: none; color: #888; font-size: 1.6em; cursor: pointer; transition: color 0.2s, transform 0.2s; line-height: 1; }
        .modal-close:hover { color: var(--red); transform: rotate(90deg); }
        .modal-body { padding: 20px; }

        /* ─── TABS ─── */
        .tab-bar { display: flex; gap: 4px; margin-bottom: 16px; background: rgba(255,255,255,0.04); border-radius: 10px; padding: 4px; flex-wrap: wrap; }
        .tab-btn { flex: 1; min-width: 60px; padding: 8px 6px; border: none; background: transparent; color: #888; font-family: 'Rajdhani', sans-serif; font-weight: 600; font-size: 0.82em; letter-spacing: 0.5px; cursor: pointer; border-radius: 7px; transition: all 0.25s; text-transform: uppercase; }
        .tab-btn.active { background: rgba(45,122,31,0.2); color: var(--green); }
        .tab-btn:hover:not(.active) { color: #ccc; }
        .tab-panel { display: none; }
        .tab-panel.active { display: block; }

        /* ─── MARKETPLACE ─── */
        .market-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(155px, 1fr)); gap: 14px; }
        .car-card { background: var(--surface); border: 1px solid rgba(45,122,31,0.2); border-radius: 12px; overflow: hidden; transition: all 0.3s; }
        .car-card:hover { border-color: rgba(45,122,31,0.6); transform: translateY(-4px); box-shadow: 0 0 20px rgba(0,255,136,0.12); }
        .car-card-img-wrap { position: relative; overflow: hidden; cursor: zoom-in; }
        .car-card-img { width: 100%; aspect-ratio: 3/4; object-fit: cover; background: #0a130a; display: block; }
        .car-card-info { padding: 10px 12px; }
        .car-card-name { font-family: 'Orbitron', sans-serif; font-size: 0.6em; font-weight: 700; color: #fff; letter-spacing: 1px; text-transform: uppercase; }
        .car-card-country { font-size: 0.85em; color: #aaa; margin: 2px 0; }
        .car-card-price { display: flex; align-items: center; gap: 5px; font-size: 0.95em; font-weight: 700; color: var(--gold); margin: 5px 0 8px; }
        .buy-car-btn { width: 100%; padding: 8px; background: linear-gradient(135deg, rgba(45,122,31,0.15), rgba(0,200,80,0.1)); border: 1px solid rgba(45,122,31,0.4); border-radius: 8px; color: var(--green); font-family: 'Rajdhani', sans-serif; font-weight: 700; font-size: 0.88em; cursor: pointer; text-transform: uppercase; transition: all 0.25s; }
        .buy-car-btn:hover { background: rgba(45,122,31,0.3); color: #fff; }
        .buy-car-btn:disabled { opacity: 0.35; cursor: not-allowed; }

        /* ─── DURABILIDAD ─── */
        .dur-bar-wrap { margin-top: 6px; }
        .dur-bar-label { display: flex; justify-content: space-between; font-size: 0.72em; color: #888; margin-bottom: 3px; }
        .dur-bar-bg { background: rgba(255,255,255,0.07); border-radius: 50px; height: 5px; overflow: hidden; }
        .dur-bar-fill { height: 100%; border-radius: 50px; transition: width 0.5s ease; }
        .dur-high { background: linear-gradient(90deg, var(--green), #00ff88cc); }
        .dur-med  { background: linear-gradient(90deg, var(--gold), #ffaa00); }
        .dur-low  { background: linear-gradient(90deg, var(--red), #ff6666); }

        /* ─── RECOMPENSA SECCIONES ─── */
        .reward-sections { display: flex; flex-direction: column; gap: 14px; margin-bottom: 14px; }
        .reward-section { background: var(--surface); border: 1px solid rgba(45,122,31,0.2); border-radius: 12px; padding: 14px; }
        .reward-section h4 { font-family: 'Orbitron', sans-serif; font-size: 0.72em; color: var(--green); letter-spacing: 2px; text-transform: uppercase; margin-bottom: 10px; }
        .reward-stat-row { display: flex; align-items: center; justify-content: space-between; margin-bottom: 8px; }
        .reward-stat-label { font-size: 0.84em; color: #aaa; display: flex; align-items: center; gap: 6px; }
        .reward-stat-value { font-weight: 700; color: var(--gold); font-size: 0.9em; }
        .pts-bar-bg { background: rgba(255,255,255,0.07); border-radius: 50px; height: 7px; overflow: hidden; margin-top: 4px; }
        .pts-bar-fill { height: 100%; border-radius: 50px; background: linear-gradient(90deg, var(--green), var(--gold)); transition: width 0.5s; }

        /* ─── RECOMPENSA DIARIA ─── */
        .reward-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 6px; }
        .reward-day { background: var(--surface); border: 1px solid rgba(255,255,255,0.07); border-radius: 9px; padding: 8px 2px; text-align: center; font-size: 0.7em; color: #888; transition: all 0.25s; }
        .reward-day.claimed { border-color: var(--green); color: var(--green); background: rgba(0,255,136,0.06); }
        .reward-day.today { border-color: var(--cyan); color: var(--cyan); box-shadow: 0 0 10px rgba(0,212,255,0.3); }
        .reward-day.gold-day { border-color: var(--gold); color: var(--gold); }
        .reward-day.gold-day.claimed { background: rgba(255,215,0,0.07); }
        .reward-day .day-icon { font-size: 1.1em; display: block; margin-bottom: 2px; }
        .claim-reward-btn { width: 100%; margin-top: 14px; padding: 12px; background: linear-gradient(135deg, rgba(0,255,136,0.12), rgba(45,122,31,0.12)); border: 1px solid var(--green); border-radius: 10px; color: var(--green); font-family: 'Orbitron', sans-serif; font-weight: 700; font-size: 0.82em; letter-spacing: 2px; cursor: pointer; text-transform: uppercase; transition: all 0.3s; }
        .claim-reward-btn:hover { background: rgba(0,255,136,0.22); color: #fff; }
        .claim-reward-btn:disabled { opacity: 0.4; cursor: not-allowed; }

        /* ─── LOGROS ─── */
        .logros-list { display: flex; flex-direction: column; gap: 10px; }
        .logro-item { display: flex; align-items: center; gap: 12px; padding: 12px 14px; background: var(--surface); border-radius: 10px; border: 1px solid rgba(255,255,255,0.06); }
        .logro-item.desbloqueado { border-color: rgba(0,255,136,0.3); background: rgba(0,255,136,0.04); }
        .logro-icon { font-size: 1.4em; }
        .logro-info { flex: 1; }
        .logro-info h4 { font-weight: 700; font-size: 0.92em; }
        .logro-info p { font-size: 0.78em; color: #888; margin-top: 2px; }
        .logro-progress { margin-top: 5px; height: 4px; background: rgba(255,255,255,0.07); border-radius: 50px; overflow: hidden; }
        .logro-progress-fill { height: 100%; background: linear-gradient(90deg, var(--green), var(--gold)); border-radius: 50px; }
        .logro-reward { text-align: right; font-weight: 700; white-space: nowrap; }

        /* ─── GARAGE / PLANTILLA ─── */
        .garage-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 9px; margin-bottom: 16px; }
        .garage-slot-card { background: var(--surface); border: 1px solid rgba(255,255,255,0.07); border-radius: 10px; padding: 11px 6px; text-align: center; font-size: 0.76em; color: #555; transition: all 0.25s; cursor: pointer; }
        .garage-slot-card.occupied { border-color: rgba(45,122,31,0.4); color: var(--green); }
        .garage-slot-card.occupied:hover { border-color: var(--green); box-shadow: 0 0 12px rgba(0,255,136,0.2); }
        .garage-slot-card .gs-icon { font-size: 1.7em; margin-bottom: 4px; }
        .garage-slot-card .gs-name { font-weight: 700; font-size: 0.88em; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

        /* ─── ENTRENAMIENTO ─── */
        .mant-item { display: flex; align-items: center; gap: 14px; padding: 14px 16px; background: var(--surface); border-radius: 12px; border: 1px solid rgba(255,255,255,0.07); margin-bottom: 12px; }
        .mant-icon { font-size: 2em; }
        .mant-info { flex: 1; }
        .mant-info h4 { font-weight: 700; font-size: 0.95em; }
        .mant-info p { font-size: 0.82em; color: #888; margin-top: 3px; }
        .mant-price { text-align: right; }
        .mant-price strong { display: block; color: var(--green); font-family: 'Orbitron', sans-serif; font-size: 0.86em; }
        .mant-price small { color: #888; font-size: 0.76em; }
        .mant-btn { margin-top: 6px; padding: 5px 14px; background: rgba(0,255,136,0.1); border: 1px solid rgba(0,255,136,0.3); border-radius: 6px; color: var(--green); font-family: 'Rajdhani', sans-serif; font-weight: 700; font-size: 0.82em; cursor: pointer; transition: all 0.25s; }
        .mant-btn:hover { background: rgba(0,255,136,0.2); }

        /* ─── FUTCANCHA (PVP) ─── */
        .duel-mode-card { padding: 20px; border-radius: 14px; border: 1px solid rgba(255,255,255,0.08); background: var(--surface); margin-bottom: 14px; cursor: pointer; transition: all 0.3s; text-align: center; }
        .duel-mode-card:hover { border-color: var(--green); box-shadow: 0 0 18px rgba(0,255,136,0.12); transform: translateY(-3px); }
        .duel-mode-card .dm-icon { font-size: 2.5em; margin-bottom: 10px; }
        .duel-mode-card h3 { font-family: 'Bebas Neue', sans-serif; font-size: 1.3em; color: var(--green); letter-spacing: 3px; margin-bottom: 8px; }
        .duel-mode-card p { font-size: 0.86em; color: #888; line-height: 1.6; }
        .prizes-row { display: flex; gap: 8px; margin-top: 12px; }
        .prize-badge { flex: 1; padding: 9px 4px; border-radius: 8px; text-align: center; font-weight: 700; font-size: 0.8em; }
        .prize-badge.gold-p   { background: rgba(255,215,0,0.1);   border: 1px solid rgba(255,215,0,0.3);   color: var(--gold); }
        .prize-badge.silver-p { background: rgba(192,192,192,0.07); border: 1px solid rgba(192,192,192,0.2); color: #bbb; }
        .prize-badge.bronze-p { background: rgba(205,127,50,0.08);  border: 1px solid rgba(205,127,50,0.25); color: #cd7f32; }

        /* ─── FUTMATCH RESULT ─── */
        .match-result-box { background: linear-gradient(135deg, #0a1a0a, #060d06); border: 1px solid rgba(0,255,136,0.3); border-radius: 16px; padding: 20px; margin-top: 14px; }
        .match-score-row { display: flex; align-items: center; justify-content: space-around; margin: 14px 0; }
        .match-team { text-align: center; }
        .match-team-flag { font-size: 2.5em; display: block; }
        .match-team-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.1em; letter-spacing: 2px; margin-top: 4px; }
        .match-score-center { font-family: 'Bebas Neue', sans-serif; font-size: 3.5em; color: var(--gold); line-height: 1; }
        .match-event { font-size: 0.82em; color: #888; padding: 4px 0; border-bottom: 1px solid rgba(255,255,255,0.04); }
        .match-event:last-child { border-bottom: none; }
        .match-event.goal { color: var(--green); font-weight: 700; }
        .match-winner-banner { text-align: center; padding: 12px; background: rgba(0,255,136,0.08); border-radius: 10px; margin-top: 10px; }
        .match-winner-banner h4 { font-family: 'Bebas Neue', sans-serif; font-size: 1.3em; color: var(--gold); letter-spacing: 3px; }

        /* PVP matchmaking */
        .pvp-search-box { text-align: center; padding: 30px 20px; }
        .pvp-search-icon { font-size: 3em; animation: spin 2s linear infinite; display: inline-block; }
        @keyframes spin { from{transform:rotate(0deg)} to{transform:rotate(360deg)} }
        .pvp-player-vs { display: flex; align-items: center; justify-content: space-around; margin: 14px 0; }
        .pvp-player-card { background: var(--surface); border: 1px solid rgba(45,122,31,0.3); border-radius: 12px; padding: 14px; text-align: center; width: 42%; }
        .pvp-vs { font-family: 'Bebas Neue', sans-serif; font-size: 2em; color: var(--gold); }
        .player-id-badge { display: inline-block; padding: 2px 8px; background: rgba(0,212,255,0.1); border: 1px solid rgba(0,212,255,0.3); border-radius: 20px; font-size: 0.7em; color: var(--cyan); margin-top: 4px; }

        /* ─── FUTSALA MAP ─── */
        .map-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
        .map-card { border-radius: 14px; overflow: hidden; border: 1px solid rgba(255,255,255,0.08); cursor: pointer; transition: all 0.3s; background: var(--surface); }
        .map-card:hover { border-color: var(--orange); box-shadow: 0 0 18px rgba(255,140,0,0.15); transform: translateY(-3px); }
        .map-card-img { width: 100%; aspect-ratio: 16/9; display: flex; align-items: center; justify-content: center; font-size: 2.8em; }
        .map-card-info { padding: 11px 13px; }
        .map-card-info h4 { font-family: 'Orbitron', sans-serif; font-size: 0.7em; color: var(--orange); letter-spacing: 1.5px; }
        .map-card-info p { font-size: 0.8em; color: #888; margin-top: 4px; line-height: 1.5; }
        .map-card-info .map-tag { display: inline-block; margin-top: 5px; padding: 2px 8px; background: rgba(0,212,255,0.1); border: 1px solid rgba(0,212,255,0.25); border-radius: 20px; font-size: 0.72em; color: var(--cyan); }
        .futsala-info-box { padding: 13px; background: rgba(0,255,136,0.06); border: 1px solid rgba(0,255,136,0.2); border-radius: 10px; font-size: 0.86em; color: var(--green); line-height: 1.7; margin-bottom: 14px; }

        /* ─── TORNEO ─── */
        .tournament-banner { background: linear-gradient(135deg, rgba(255,140,0,0.1), rgba(255,68,68,0.1)); border: 1px solid rgba(255,140,0,0.3); border-radius: 14px; padding: 18px; text-align: center; margin-bottom: 16px; }
        .tournament-banner h3 { font-family: 'Bebas Neue', sans-serif; color: var(--orange); margin-bottom: 8px; letter-spacing: 3px; font-size: 1.5em; }
        .tournament-banner .t-prize { font-size: 1.8em; font-weight: 700; color: var(--gold); }
        .tournament-entry { background: rgba(0,212,255,0.05); border: 1px solid rgba(0,212,255,0.2); border-radius: 12px; padding: 18px; margin-top: 14px; text-align: center; }
        .tournament-entry h4 { font-family: 'Orbitron', sans-serif; font-size: 0.82em; color: var(--cyan); letter-spacing: 1.5px; margin-bottom: 10px; }
        .tournament-entry p { font-size: 0.86em; color: #888; margin-bottom: 14px; line-height: 1.6; }
        .torneo-pay-btn { padding: 13px 26px; background: linear-gradient(135deg, rgba(255,215,0,0.12), rgba(255,140,0,0.12)); border: 1px solid var(--gold); border-radius: 10px; color: var(--gold); font-family: 'Orbitron', sans-serif; font-weight: 700; font-size: 0.82em; letter-spacing: 2px; cursor: pointer; text-transform: uppercase; transition: all 0.3s; }
        .torneo-pay-btn:hover { background: rgba(255,215,0,0.22); color: #fff; }

        /* ─── RANKING ─── */
        .rank-list { list-style: none; display: flex; flex-direction: column; gap: 10px; }
        .rank-item { display: flex; align-items: center; gap: 12px; padding: 12px 14px; background: var(--surface); border-radius: 10px; border: 1px solid rgba(255,255,255,0.06); }
        .rank-pos { font-family: 'Orbitron', sans-serif; font-size: 1em; color: var(--green); font-weight: 700; width: 28px; text-align: center; }
        .rank-name { flex: 1; font-weight: 600; font-size: 0.93em; }
        .rank-score { color: var(--gold); font-weight: 700; font-size: 0.88em; white-space: nowrap; }

        /* ─── DEPOSITO / RETIRO ─── */
        .withdraw-label { font-size: 0.84em; color: #888; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 6px; display: block; }
        .withdraw-input { width: 100%; padding: 11px 14px; background: rgba(255,255,255,0.05); border: 1px solid rgba(45,122,31,0.3); border-radius: 10px; color: #fff; font-size: 0.95em; font-family: 'Rajdhani', sans-serif; outline: none; transition: border-color 0.3s; margin-bottom: 14px; }
        .withdraw-input:focus { border-color: var(--green); }
        .withdraw-btn { width: 100%; padding: 13px; background: linear-gradient(135deg, rgba(255,140,0,0.12), rgba(255,68,68,0.12)); border: 1px solid var(--orange); border-radius: 10px; color: var(--orange); font-family: 'Orbitron', sans-serif; font-weight: 700; letter-spacing: 2px; font-size: 0.82em; cursor: pointer; text-transform: uppercase; transition: all 0.3s; }
        .withdraw-btn:hover { background: rgba(255,140,0,0.22); color: #fff; }
        .gem-tiers { display: flex; flex-direction: column; gap: 11px; }
        .gem-tier { display: flex; align-items: center; justify-content: space-between; padding: 13px 16px; background: var(--surface); border-radius: 12px; border: 1px solid rgba(255,215,0,0.15); cursor: pointer; transition: all 0.3s; }
        .gem-tier:hover { border-color: var(--gold); box-shadow: 0 0 12px rgba(255,215,0,0.1); }
        .gem-tier-left { display: flex; align-items: center; gap: 12px; }
        .gem-tier-icon { font-size: 1.8em; }
        .gem-tier-info h4 { font-weight: 700; color: var(--gold); font-size: 0.9em; }
        .gem-tier-info p { font-size: 0.78em; color: #888; margin-top: 2px; }
        .gem-tier-price { font-family: 'Orbitron', sans-serif; font-size: 0.84em; color: var(--cyan); font-weight: 700; }

        /* ─── NIVEL ─── */
        .nivel-bar-wrap { margin: 14px 0; }
        .nivel-bar-label { display: flex; justify-content: space-between; font-size: 0.84em; color: #888; margin-bottom: 7px; }
        .nivel-bar-bg { background: rgba(255,255,255,0.07); border-radius: 50px; height: 10px; overflow: hidden; }
        .nivel-bar-fill { height: 100%; width: 35%; background: linear-gradient(90deg, var(--green), var(--gold)); border-radius: 50px; }
        .nivel-perks { display: flex; flex-direction: column; gap: 9px; margin-top: 16px; }
        .nivel-perk { display: flex; align-items: center; gap: 10px; padding: 10px; background: var(--surface); border-radius: 10px; font-size: 0.88em; }

        /* ─── PLAYER DETAIL MODAL ─── */
        .car-detail-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.95); backdrop-filter: blur(16px); z-index: 1100; justify-content: center; align-items: center; padding: 20px; }
        .car-detail-overlay.active { display: flex; }
        .car-detail-modal { background: linear-gradient(145deg, #0e1a0e 0%, #080808 100%); border: 1px solid rgba(45,122,31,0.4); border-radius: 22px; max-width: 520px; width: 100%; max-height: 92vh; overflow-y: auto; box-shadow: 0 0 80px rgba(0,255,136,0.15); animation: modalIn 0.35s cubic-bezier(0.175,0.885,0.32,1.275); }
        .car-detail-img-wrap { position: relative; width: 100%; }
        .car-detail-img { width: 100%; aspect-ratio: 3/4; object-fit: cover; border-radius: 20px 20px 0 0; display: block; }
        .car-detail-img-glow { position: absolute; bottom: 0; left: 0; right: 0; height: 80px; background: linear-gradient(to top, #0e1a0e, transparent); }
        .car-detail-close { position: absolute; top: 12px; right: 12px; background: rgba(0,0,0,0.7); border: 1px solid rgba(255,255,255,0.15); border-radius: 50%; width: 34px; height: 34px; display: flex; align-items: center; justify-content: center; color: #aaa; font-size: 1.1em; cursor: pointer; transition: all 0.2s; z-index: 2; }
        .car-detail-close:hover { color: var(--red); border-color: var(--red); transform: rotate(90deg); }
        .player-id-display { position: absolute; top: 12px; left: 12px; background: rgba(0,0,0,0.8); border: 1px solid rgba(0,212,255,0.4); border-radius: 8px; padding: 4px 10px; font-size: 0.7em; color: var(--cyan); font-family: 'Orbitron', sans-serif; z-index: 2; }
        .car-detail-body { padding: 18px 22px 24px; }
        .car-detail-name { font-family: 'Bebas Neue', sans-serif; font-size: 1.8em; color: #fff; letter-spacing: 3px; text-transform: uppercase; margin-bottom: 4px; }
        .car-detail-badge { display: inline-block; padding: 3px 10px; background: rgba(45,122,31,0.1); border: 1px solid rgba(45,122,31,0.4); border-radius: 20px; font-size: 0.72em; color: var(--green); letter-spacing: 1px; text-transform: uppercase; margin-bottom: 16px; }
        .car-detail-stats { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 16px; }
        .car-stat-card { background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.07); border-radius: 12px; padding: 12px 14px; }
        .car-stat-card .stat-icon { font-size: 1.4em; margin-bottom: 4px; }
        .car-stat-card .stat-label { font-size: 0.72em; color: #888; text-transform: uppercase; letter-spacing: 0.8px; }
        .car-stat-card .stat-value { font-family: 'Orbitron', sans-serif; font-size: 1.05em; font-weight: 700; margin-top: 2px; }
        .car-stat-card.stat-hp .stat-value { color: var(--green); }
        .car-stat-card.stat-dur .stat-value { color: var(--cyan); }
        .car-stat-card.stat-fuel .stat-value { color: var(--orange); }
        .car-stat-card.stat-earn .stat-value { color: var(--gold); }
        .car-detail-divider { height: 1px; background: rgba(255,255,255,0.06); margin: 14px 0; }

        /* ─── INFO / MISC ─── */
        .info-box { padding: 11px 14px; background: rgba(255,140,0,0.07); border: 1px solid rgba(255,140,0,0.2); border-radius: 10px; font-size: 0.83em; color: #ffaa00; line-height: 1.6; }
        .coming-soon-box { padding: 12px; background: var(--surface); border-radius: 10px; text-align: center; color: #666; font-size: 0.84em; margin-top: 12px; }
        .futsala-match-loading { text-align: center; padding: 24px; }
        .pitch-loader { font-size: 3em; animation: bounce 0.8s ease-in-out infinite alternate; }
        @keyframes bounce { from{transform:translateY(0)} to{transform:translateY(-16px)} }

        /* ─── SCROLLBAR ─── */
        .modal::-webkit-scrollbar { width: 4px; }
        .modal::-webkit-scrollbar-track { background: transparent; }
        .modal::-webkit-scrollbar-thumb { background: rgba(45,122,31,0.4); border-radius: 5px; }

        /* ─── RESPONSIVE ─── */
        @media (max-width: 900px) { .layout-grid { flex-direction: column; gap: 35px; } .menu-column { width: 100%; display: grid; grid-template-columns: repeat(2,1fr); gap: 16px; } .car-container { order: -1; max-width: 88%; } }
        @media (max-width: 580px) { .market-grid { grid-template-columns: repeat(2,1fr); } .map-grid { grid-template-columns: 1fr; } .garage-grid { grid-template-columns: repeat(3,1fr); } .reward-grid { grid-template-columns: repeat(5,1fr); } .prizes-row { flex-direction: column; } }
        @media (max-width: 500px) { .menu-column { grid-template-columns: 1fr; text-align: center; } .top-bar { justify-content: center; flex-direction: column; text-align: center; gap: 14px; } .stats-container { justify-content: center; gap: 10px; } h1 { font-size: 2.5em; } .lang-bar { justify-content: center; } }
    </style>
</head>
<body>

<!-- ANIMATED FLAGS BACKGROUND -->
<div class="flags-bg" id="flags-bg"></div>

<div class="content-wrap">

<!-- LANGUAGE BAR -->
<div class="lang-bar">
    <span>🌍</span>
    <button class="lang-btn active" onclick="setLang('es')">🇪🇸 ES</button>
    <button class="lang-btn" onclick="setLang('en')">🇬🇧 EN</button>
    <button class="lang-btn" onclick="setLang('pt')">🇵🇹 PT</button>
    <button class="lang-btn" onclick="setLang('fr')">🇫🇷 FR</button>
    <button class="lang-btn" onclick="setLang('de')">🇩🇪 DE</button>
    <button class="lang-btn" onclick="setLang('it')">🇮🇹 IT</button>
    <button class="lang-btn" onclick="setLang('ja')">🇯🇵 JA</button>
    <button class="lang-btn" onclick="setLang('sv')">🇸🇪 SV</button>
</div>

<!-- TOP BAR -->
<div class="top-bar">
    <div class="stats-container">
        <span data-i18n="pts">Puntos: <strong id="puntaje">0</strong></span>
        <span data-i18n="lvl">Nivel: <strong id="nivel-stat">1</strong></span>
        <span>Gemas: <strong id="gemas-stat">3900.0</strong> 💎</span>
        <span data-i18n="form">Forma: <strong id="gasolina">100%</strong></span>
        <span data-i18n="overall">Overall: <strong id="hp-stat">0</strong></span>
    </div>
    <div id="ton-connect-button"></div>
</div>

<!-- MAIN CONTENT -->
<div class="main-content">
    <div class="title-container">
        <h1>⚽ FUTMUNDI ⚽</h1>
        <div class="title-sub" data-i18n="tagline">El juego de fútbol NFT más épico del mundo</div>
    </div>

    <div class="layout-grid">
        <!-- LEFT MENU -->
        <div class="menu-column">
            <button class="word-btn" onclick="openModal('modal-garage')">👕 <span data-i18n="btn_futbolistas">Futbolistas</span></button>
            <button class="word-btn" onclick="openModal('modal-marketplace')">🏪 <span data-i18n="btn_market">Marketplace</span></button>
            <button class="word-btn" onclick="openModal('modal-recompensa-pts')">⚽ <span data-i18n="btn_rewards">Recompensas</span></button>
            <button class="word-btn" onclick="openModal('modal-recompensa')">🎁 <span data-i18n="btn_daily">Recompensa Diaria</span></button>
            <button class="word-btn" onclick="openModal('modal-logros')">🏅 <span data-i18n="btn_logros">Logros</span></button>
            <button class="word-btn" onclick="openModal('modal-nivel')">⬆️ <span data-i18n="btn_nivel">Nivel</span></button>
        </div>

        <!-- PLAYER DISPLAY -->
        <div class="car-container">
            <div class="car-base-glow"></div>
            <img class="car-image" id="main-car-img"
                 src="https://raw.githubusercontent.com/squadgamernft-star/TurboGarage/main/img/carro_320.jpg.png"
                 alt="Tu jugador activo"
                 onclick="if(carrosComprados.length>0){openCarDetail(carrosComprados[0],true)}else{mostrarMensaje('⚠️ Compra un futbolista en el Marketplace','#ffaa00')}"
                 onerror="this.src='data:image/svg+xml,'+encodeURIComponent('<svg xmlns=\'http://www.w3.org/2000/svg\' viewBox=\'0 0 460 560\'><rect fill=\'%230a130a\' width=\'460\' height=\'560\'/><text x=\'230\' y=\'300\' text-anchor=\'middle\' font-size=\'120\' fill=\'%23333\'>⚽</text></svg>')">

            <!-- Soccer Ball Stamina -->
            <div>
                <div class="fuel-label">⚽ <span data-i18n="stamina">Balones de Estamina</span></div>
                <div class="fuel-cartridges" id="fuel-cartridges">
                    <div class="fuel-cartridge full" id="fuel-0" onclick="usarCombustible(0)" title="Balón 1">⚽</div>
                    <div class="fuel-cartridge full" id="fuel-1" onclick="usarCombustible(1)" title="Balón 2">⚽</div>
                    <div class="fuel-cartridge full" id="fuel-2" onclick="usarCombustible(2)" title="Balón 3">⚽</div>
                    <div class="fuel-cartridge full" id="fuel-3" onclick="usarCombustible(3)" title="Balón 4">⚽</div>
                </div>
            </div>

            <button class="primary-btn" onclick="openModal('modal-duelo-urbano')">🏟️ <span data-i18n="btn_futcancha">FutCancha — PVP</span></button>
        </div>

        <!-- RIGHT MENU -->
        <div class="menu-column">
            <button class="word-btn" onclick="openModal('modal-duelo-callejero')">🏃 <span data-i18n="btn_futsala">Futsala — PVE</span></button>
            <button class="word-btn" onclick="openModal('modal-torneo')">🏆 <span data-i18n="btn_torneo">Torneo</span></button>
            <button class="word-btn" onclick="openModal('modal-ranking')">📊 <span data-i18n="btn_ranking">Ranking</span></button>
            <button class="word-btn" onclick="openModal('modal-mantenimiento')">🏋️ <span data-i18n="btn_entrenamiento">Entrenamiento</span></button>
            <button class="word-btn" onclick="openModal('modal-deposito')">💎 <span data-i18n="btn_deposito">Depósito</span></button>
            <button class="word-btn" onclick="openModal('modal-retiro')">💸 <span data-i18n="btn_retiro">Retiro</span></button>
        </div>
    </div>

    <div id="status-msg"></div>
</div>

<!-- ══════════ MODALES ══════════ -->

<!-- MARKETPLACE -->
<div class="modal-overlay" id="modal-marketplace">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏪 Marketplace</span>
            <button class="modal-close" onclick="closeModal('modal-marketplace')">✕</button>
        </div>
        <div class="modal-body">
            <p style="color:#888;font-size:0.86em;margin-bottom:14px;">Gemas: <strong id="market-gemas-display" style="color:var(--gold)">0.0 💎</strong></p>
            <div class="tab-bar">
                <button class="tab-btn active" onclick="switchTab(event,'marketplace','futbolistas')">🧑 Futbolistas</button>
                <button class="tab-btn" onclick="switchTab(event,'marketplace','uniformes')">👕 Uniformes</button>
                <button class="tab-btn" onclick="switchTab(event,'marketplace','tenis')">👟 Tenis</button>
                <button class="tab-btn" onclick="switchTab(event,'marketplace','protectores')">🦵 Protectores</button>
            </div>
            <div id="marketplace-futbolistas" class="tab-panel active">
                <div class="market-grid" id="car-market-grid"></div>
            </div>
            <div id="marketplace-uniformes" class="tab-panel">
                <div class="coming-soon-box">🔜 Uniformes de equipos exclusivos — próximamente</div>
            </div>
            <div id="marketplace-tenis" class="tab-panel">
                <div class="coming-soon-box">👟 Tenis de marcas premium — próximamente</div>
            </div>
            <div id="marketplace-protectores" class="tab-panel">
                <div class="coming-soon-box">🦵 Protectores y equipamiento — próximamente</div>
            </div>
        </div>
    </div>
</div>

<!-- RECOMPENSAS POR PARTIDAS -->
<div class="modal-overlay" id="modal-recompensa-pts">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">⚽ Recompensas por Estadísticas</span>
            <button class="modal-close" onclick="closeModal('modal-recompensa-pts')">✕</button>
        </div>
        <div class="modal-body">
            <p style="color:#888;font-size:0.84em;margin-bottom:14px;">Juega partidas y acumula 100 puntos de estadísticas para desbloquear recompensas. Cada partida jugada = 2 pts de estadística.</p>
            <div id="reward-pts-sections"></div>
            <div class="info-box">⚽ Progreso actual: <strong id="current-pts-display" style="color:var(--green)">0</strong> / 100 puntos de estadística</div>
        </div>
    </div>
</div>

<!-- RECOMPENSA DIARIA -->
<div class="modal-overlay" id="modal-recompensa">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🎁 Recompensa Diaria</span>
            <button class="modal-close" onclick="closeModal('modal-recompensa')">✕</button>
        </div>
        <div class="modal-body">
            <p style="color:#888;margin-bottom:12px;font-size:0.88em;">💎 Días impares: gemas &nbsp;|&nbsp; 🪙 Días pares: moneda oro</p>
            <div class="reward-grid" id="reward-days-grid"></div>
            <button class="claim-reward-btn" id="btn-claim-reward" onclick="claimReward()">✅ Reclamar Recompensa</button>
        </div>
    </div>
</div>

<!-- LOGROS -->
<div class="modal-overlay" id="modal-logros">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏅 Logros</span>
            <button class="modal-close" onclick="closeModal('modal-logros')">✕</button>
        </div>
        <div class="modal-body">
            <div class="tab-bar">
                <button class="tab-btn active" onclick="switchTab(event,'logros','jugadores')">🧑 Jugadores</button>
                <button class="tab-btn" onclick="switchTab(event,'logros','partidas')">⚽ Partidas</button>
                <button class="tab-btn" onclick="switchTab(event,'logros','goles')">🥅 Goles</button>
                <button class="tab-btn" onclick="switchTab(event,'logros','racha')">📅 Racha</button>
            </div>
            <div id="logros-jugadores" class="tab-panel active"><div class="logros-list" id="logros-jugadores-list"></div></div>
            <div id="logros-partidas" class="tab-panel"><div class="logros-list" id="logros-partidas-list"></div></div>
            <div id="logros-goles" class="tab-panel"><div class="logros-list" id="logros-goles-list"></div></div>
            <div id="logros-racha" class="tab-panel"><div class="logros-list" id="logros-racha-list"></div></div>
        </div>
    </div>
</div>

<!-- NIVEL -->
<div class="modal-overlay" id="modal-nivel">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">⬆️ Nivel</span>
            <button class="modal-close" onclick="closeModal('modal-nivel')">✕</button>
        </div>
        <div class="modal-body">
            <div style="text-align:center;margin-bottom:18px;">
                <div style="font-family:'Bebas Neue',sans-serif;font-size:4em;color:var(--green);" id="modal-nivel-num">1</div>
                <div style="color:#888;font-size:0.86em;margin-top:4px;">Nivel actual</div>
            </div>
            <div class="nivel-bar-wrap">
                <div class="nivel-bar-label"><span>XP: 350</span><span>Siguiente: 1000 XP</span></div>
                <div class="nivel-bar-bg"><div class="nivel-bar-fill"></div></div>
            </div>
            <div class="nivel-perks">
                <div class="nivel-perk"><span>✅</span><span>Nivel 1 — Acceso a Futsala (PVE)</span></div>
                <div class="nivel-perk"><span>✅</span><span>Nivel 2 — Desbloquea FutCancha (PVP)</span></div>
                <div class="nivel-perk" style="opacity:0.4"><span>🔒</span><span>Nivel 3 — Torneos disponibles</span></div>
                <div class="nivel-perk" style="opacity:0.4"><span>🔒</span><span>Nivel 5 — Mercado de equipamiento exclusivo</span></div>
                <div class="nivel-perk" style="opacity:0.4"><span>🔒</span><span>Nivel 10 — Liga Profesional</span></div>
            </div>
        </div>
    </div>
</div>

<!-- RETIRO -->
<div class="modal-overlay" id="modal-retiro">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">💸 Retiro de Gemas</span>
            <button class="modal-close" onclick="closeModal('modal-retiro')">✕</button>
        </div>
        <div class="modal-body">
            <label class="withdraw-label">Dirección TON Wallet</label>
            <input class="withdraw-input" type="text" id="retiro-address" placeholder="UQ...">
            <label class="withdraw-label">Cantidad de gemas 💎</label>
            <input class="withdraw-input" type="number" id="retiro-cantidad" placeholder="Mínimo 500 gemas">
            <div class="info-box" style="margin-bottom:14px;">⚠️ Mínimo: 500 💎 &nbsp;·&nbsp; 1000💎 = 0.1 TON &nbsp;·&nbsp; Proceso: 24h</div>
            <button class="withdraw-btn" onclick="retirar()">🚀 Solicitar Retiro</button>
        </div>
    </div>
</div>

<!-- FUTCANCHA PVP -->
<div class="modal-overlay" id="modal-duelo-urbano">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏟️ FutCancha — PVP</span>
            <button class="modal-close" onclick="closeModal('modal-duelo-urbano')">✕</button>
        </div>
        <div class="modal-body">
            <div id="pvp-content">
                <div class="duel-mode-card" onclick="buscarPartidaPVP('liga')">
                    <div class="dm-icon">🏆</div>
                    <h3>Liga Mundial</h3>
                    <p>Enfrenta a jugadores en línea con la misma categoría de NFT y rango de Overall. Emparejamiento automático por nivel.</p>
                    <div class="prizes-row">
                        <div class="prize-badge gold-p">🥇 Victoria<br><small>+5 pts estam.</small></div>
                        <div class="prize-badge silver-p">🤝 Empate<br><small>+2 pts estam.</small></div>
                        <div class="prize-badge bronze-p">❌ Derrota<br><small>+1 pt estam.</small></div>
                    </div>
                </div>
                <div class="duel-mode-card" onclick="buscarPartidaPVP('amistoso')">
                    <div class="dm-icon">⚽</div>
                    <h3>Partido Amistoso</h3>
                    <p>Sin penalización. Practica contra otros jugadores. Gana puntos de estadística en cada partido.</p>
                    <div class="prizes-row">
                        <div class="prize-badge gold-p">🥇 Victoria<br><small>+3 pts estam.</small></div>
                        <div class="prize-badge silver-p">🤝 Empate<br><small>+1 pt estam.</small></div>
                        <div class="prize-badge bronze-p">❌ Derrota<br><small>+1 pt estam.</small></div>
                    </div>
                </div>
                <div class="coming-soon-box">⏳ Emparejamiento en línea — próximamente</div>
            </div>
        </div>
    </div>
</div>

<!-- ENTRENAMIENTO -->
<div class="modal-overlay" id="modal-mantenimiento">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏋️ Entrenamiento</span>
            <button class="modal-close" onclick="closeModal('modal-mantenimiento')">✕</button>
        </div>
        <div class="modal-body">
            <p style="color:#888;font-size:0.86em;margin-bottom:16px;">El entrenamiento recupera la estamina al 100%, mejora el rendimiento +10% y repara el estado físico del jugador.</p>
            <div id="mant-list"></div>
            <div class="info-box" style="margin-top:4px;">💡 El entrenamiento restaura los 4 balones de estamina, mejora el Overall del jugador +10% y sana su forma física.</div>
        </div>
    </div>
</div>

<!-- PLANTILLA (GARAGE) -->
<div class="modal-overlay" id="modal-garage">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">👕 Mi Plantilla</span>
            <button class="modal-close" onclick="closeModal('modal-garage')">✕</button>
        </div>
        <div class="modal-body">
            <div class="tab-bar">
                <button class="tab-btn active" onclick="switchTab(event,'garage','vehiculos')">🧑 Futbolistas</button>
                <button class="tab-btn" onclick="switchTab(event,'garage','cascos')">👕 Uniformes</button>
                <button class="tab-btn" onclick="switchTab(event,'garage','guantes')">👟 Tenis</button>
                <button class="tab-btn" onclick="switchTab(event,'garage','botas')">🦵 Protectores</button>
                <button class="tab-btn" onclick="switchTab(event,'garage','trajes')">🏅 Insignias</button>
            </div>
            <div id="garage-vehiculos" class="tab-panel active">
                <p style="color:#888;font-size:0.82em;margin-bottom:10px;">20 slots de plantilla disponibles</p>
                <div class="garage-grid" id="garage-grid-vehicles"></div>
                <button class="primary-btn" onclick="closeModal('modal-garage');openModal('modal-marketplace')">🏪 Ir al Marketplace</button>
            </div>
            <div id="garage-cascos" class="tab-panel"><div class="coming-soon-box">⏳ Uniformes próximamente</div></div>
            <div id="garage-guantes" class="tab-panel"><div class="coming-soon-box">⏳ Tenis próximamente</div></div>
            <div id="garage-botas" class="tab-panel"><div class="coming-soon-box">⏳ Protectores próximamente</div></div>
            <div id="garage-trajes" class="tab-panel"><div class="coming-soon-box">⏳ Insignias próximamente</div></div>
        </div>
    </div>
</div>

<!-- RANKING -->
<div class="modal-overlay" id="modal-ranking">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏆 Ranking Global</span>
            <button class="modal-close" onclick="closeModal('modal-ranking')">✕</button>
        </div>
        <div class="modal-body">
            <ul class="rank-list">
                <li class="rank-item"><span class="rank-pos">🥇</span><span class="rank-name">🇧🇷 Neymarfan_BR</span><span class="rank-score">48,320 pts</span></li>
                <li class="rank-item"><span class="rank-pos">🥈</span><span class="rank-name">🇦🇷 Messi10_AR</span><span class="rank-score">41,100 pts</span></li>
                <li class="rank-item"><span class="rank-pos">🥉</span><span class="rank-name">🇫🇷 KylianK_FR</span><span class="rank-score">39,750 pts</span></li>
                <li class="rank-item"><span class="rank-pos" style="color:#aaa">4</span><span class="rank-name">🇩🇪 FutbolKing_DE</span><span class="rank-score">35,200 pts</span></li>
                <li class="rank-item"><span class="rank-pos" style="color:#aaa">5</span><span class="rank-name">🇪🇸 LaRoja_Pro</span><span class="rank-score">31,800 pts</span></li>
                <li class="rank-item" style="border-color:rgba(0,255,136,0.3);background:rgba(0,255,136,0.04)">
                    <span class="rank-pos" style="color:#aaa">—</span><span class="rank-name" style="color:var(--green)">🙋 Tú</span><span class="rank-score" id="my-rank-score">0 pts</span>
                </li>
            </ul>
        </div>
    </div>
</div>

<!-- DEPOSITO -->
<div class="modal-overlay" id="modal-deposito">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">💎 Depósito de Gemas</span>
            <button class="modal-close" onclick="closeModal('modal-deposito')">✕</button>
        </div>
        <div class="modal-body">
            <p style="color:#888;margin-bottom:14px;font-size:0.86em;">Compra gemas con TON para fichar futbolistas en el marketplace.</p>
            <div class="gem-tiers">
                <div class="gem-tier" onclick="comprarGemas(100,0.1)"><div class="gem-tier-left"><span class="gem-tier-icon">💎</span><div class="gem-tier-info"><h4>100 Gemas</h4><p>Pack básico</p></div></div><span class="gem-tier-price">0.1 TON</span></div>
                <div class="gem-tier" onclick="comprarGemas(500,0.45)"><div class="gem-tier-left"><span class="gem-tier-icon">💎💎</span><div class="gem-tier-info"><h4>500 Gemas</h4><p>Ahorra 10% · Popular</p></div></div><span class="gem-tier-price">0.45 TON</span></div>
                <div class="gem-tier" onclick="comprarGemas(1200,1.0)"><div class="gem-tier-left"><span class="gem-tier-icon">💎💎💎</span><div class="gem-tier-info"><h4>1,200 Gemas</h4><p>Ahorra 16%</p></div></div><span class="gem-tier-price">1 TON</span></div>
                <div class="gem-tier" onclick="comprarGemas(3000,2.0)"><div class="gem-tier-left"><span class="gem-tier-icon">🏆</span><div class="gem-tier-info"><h4>3,000 Gemas</h4><p>Ahorra 25% · PRO</p></div></div><span class="gem-tier-price">2 TON</span></div>
            </div>
        </div>
    </div>
</div>

<!-- FUTSALA PVE -->
<div class="modal-overlay" id="modal-duelo-callejero">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏃 Futsala — PVE</span>
            <button class="modal-close" onclick="closeModal('modal-duelo-callejero')">✕</button>
        </div>
        <div class="modal-body" id="futsala-body">
            <div class="futsala-info-box">
                ✅ Modo GRATUITO — Sin gemas &nbsp;·&nbsp; 🤖 Vs Inteligencia Artificial &nbsp;·&nbsp; +2 pts estadísticas por partida
            </div>
            <p style="color:#888;font-size:0.84em;margin-bottom:12px;">Elige una cancha y compite contra la IA:</p>
            <div class="map-grid">
                <div class="map-card" onclick="iniciarFutsala('Cancha Barrial')">
                    <div class="map-card-img" style="background:linear-gradient(135deg,#0a1a0a,#1a3a1a)">🏟️</div>
                    <div class="map-card-info"><h4>Cancha Barrial</h4><p>Partidos rápidos en cancha local. IA nivel fácil. Ideal para comenzar.</p><span class="map-tag">⭐ Fácil · +2 pts</span></div>
                </div>
                <div class="map-card" onclick="iniciarFutsala('Estadio Municipal')">
                    <div class="map-card-img" style="background:linear-gradient(135deg,#1a1a0a,#3a2a0a)">🏟️</div>
                    <div class="map-card-info"><h4>Estadio Municipal</h4><p>IA de nivel medio. Tácticas variadas y mayor presión defensiva.</p><span class="map-tag">⭐⭐ Medio · +2 pts</span></div>
                </div>
                <div class="map-card" onclick="iniciarFutsala('Copa del Mundo')">
                    <div class="map-card-img" style="background:linear-gradient(135deg,#1a0a0a,#3a0a0a)">🏆</div>
                    <div class="map-card-info"><h4>Copa del Mundo</h4><p>IA élite. Alta dificultad. Más puntos de estadística al ganar.</p><span class="map-tag">⭐⭐⭐ Difícil · +2 pts</span></div>
                </div>
                <div class="map-card" onclick="iniciarFutsala('Noche de Gala')">
                    <div class="map-card-img" style="background:linear-gradient(135deg,#0a0a1a,#0a1a3a)">🌃</div>
                    <div class="map-card-info"><h4>Noche de Gala</h4><p>Estadio nocturno. IA con jugadas maestras. El reto máximo.</p><span class="map-tag">⭐⭐⭐⭐ Experto · +2 pts</span></div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- TORNEO -->
<div class="modal-overlay" id="modal-torneo">
    <div class="modal">
        <div class="modal-header">
            <span class="modal-title">🏆 Torneo</span>
            <button class="modal-close" onclick="closeModal('modal-torneo')">✕</button>
        </div>
        <div class="modal-body">
            <div class="tournament-banner">
                <h3>GRAN COPA FUTMUNDI</h3>
                <div class="t-prize">🏆 500 💎 + Jugador Legendario NFT</div>
                <p style="color:#888;margin-top:8px;font-size:0.84em;">Termina en: 3d 14h 22m</p>
            </div>
            <ul class="rank-list" style="margin-bottom:16px;">
                <li class="rank-item"><span class="rank-pos">🥇</span><span class="rank-name">1er Lugar</span><span class="rank-score">250💎 + Jugador NFT Oro</span></li>
                <li class="rank-item"><span class="rank-pos">🥈</span><span class="rank-name">2do Lugar</span><span class="rank-score">150💎</span></li>
                <li class="rank-item"><span class="rank-pos">🥉</span><span class="rank-name">3er Lugar</span><span class="rank-score">100💎</span></li>
                <li class="rank-item"><span class="rank-pos" style="color:#aaa">4–10</span><span class="rank-name">Top 10</span><span class="rank-score">10💎</span></li>
            </ul>
            <div class="tournament-entry">
                <h4>💳 INSCRIPCIÓN</h4>
                <p>Costo: <strong style="color:var(--gold)">$5 USD en USDT</strong><br>Desde tu wallet TON conectada.</p>
                <button class="torneo-pay-btn" onclick="pagarTorneo()">💳 Pagar $5 USDT — Inscribirme</button>
            </div>
            <div class="coming-soon-box">⏳ Torneo disponible próximamente</div>
        </div>
    </div>
</div>

<!-- PLAYER DETAIL MODAL -->
<div class="car-detail-overlay" id="modal-car-detail">
    <div class="car-detail-modal">
        <div class="car-detail-img-wrap">
            <img class="car-detail-img" id="car-detail-img" src="" alt="">
            <div class="car-detail-img-glow"></div>
            <div class="player-id-display" id="car-detail-player-id">ID: #000</div>
            <button class="car-detail-close" onclick="closeCarDetail()">✕</button>
        </div>
        <div class="car-detail-body">
            <div class="car-detail-name" id="car-detail-name"></div>
            <div style="font-size:1.2em;margin-bottom:6px;" id="car-detail-country-flag"></div>
            <div class="car-detail-badge">NFT · TON BLOCKCHAIN · FUTMUNDI</div>
            <div class="car-detail-stats">
                <div class="car-stat-card stat-hp">
                    <div class="stat-icon">⚡</div>
                    <div class="stat-label">Overall</div>
                    <div class="stat-value" id="car-detail-hp">—</div>
                </div>
                <div class="car-stat-card stat-dur">
                    <div class="stat-icon">💪</div>
                    <div class="stat-label">Forma Física</div>
                    <div class="stat-value" id="car-detail-dur">100%</div>
                </div>
                <div class="car-stat-card stat-fuel">
                    <div class="stat-icon">⚽</div>
                    <div class="stat-label">Estamina</div>
                    <div class="stat-value" id="car-detail-fuel">4 / 4</div>
                </div>
                <div class="car-stat-card stat-earn">
                    <div class="stat-icon">💎</div>
                    <div class="stat-label">Gemas / día</div>
                    <div class="stat-value" id="car-detail-earn">—</div>
                </div>
            </div>
            <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:14px;" id="car-detail-extra-stats"></div>
            <div class="car-detail-divider"></div>
            <div id="car-detail-dur-bar" style="margin-bottom:12px"></div>
            <div id="car-detail-fuel-dots" style="display:flex;gap:10px;justify-content:center;margin-bottom:14px"></div>
            <div id="car-detail-action-btn"></div>
        </div>
    </div>
</div>

<!-- ══════════ JAVASCRIPT ══════════ -->
<script>

// ── IDIOMAS ──────────────────────────────────────────────────
const TRANSLATIONS = {
    es: { tagline:'El juego de fútbol NFT más épico del mundo', btn_futbolistas:'Futbolistas', btn_market:'Marketplace', btn_rewards:'Recompensas', btn_daily:'Recompensa Diaria', btn_logros:'Logros', btn_nivel:'Nivel', btn_futsala:'Futsala — PVE', btn_torneo:'Torneo', btn_ranking:'Ranking', btn_entrenamiento:'Entrenamiento', btn_deposito:'Depósito', btn_retiro:'Retiro', btn_futcancha:'FutCancha — PVP', stamina:'Balones de Estamina', pts:'Puntos', lvl:'Nivel', form:'Forma', overall:'Overall' },
    en: { tagline:'The most epic NFT soccer game in the world', btn_futbolistas:'Players', btn_market:'Marketplace', btn_rewards:'Rewards', btn_daily:'Daily Reward', btn_logros:'Achievements', btn_nivel:'Level', btn_futsala:'Futsala — PVE', btn_torneo:'Tournament', btn_ranking:'Ranking', btn_entrenamiento:'Training', btn_deposito:'Deposit', btn_retiro:'Withdraw', btn_futcancha:'FutCancha — PVP', stamina:'Stamina Balls', pts:'Points', lvl:'Level', form:'Form', overall:'Overall' },
    pt: { tagline:'O jogo de futebol NFT mais épico do mundo', btn_futbolistas:'Jogadores', btn_market:'Mercado', btn_rewards:'Recompensas', btn_daily:'Recompensa Diária', btn_logros:'Conquistas', btn_nivel:'Nível', btn_futsala:'Futsala — PVE', btn_torneo:'Torneio', btn_ranking:'Ranking', btn_entrenamiento:'Treinamento', btn_deposito:'Depósito', btn_retiro:'Retirada', btn_futcancha:'FutCancha — PVP', stamina:'Bolas de Estâmina', pts:'Pontos', lvl:'Nível', form:'Forma', overall:'Overall' },
    fr: { tagline:'Le jeu de football NFT le plus épique du monde', btn_futbolistas:'Joueurs', btn_market:'Marché', btn_rewards:'Récompenses', btn_daily:'Récompense Quotidienne', btn_logros:'Succès', btn_nivel:'Niveau', btn_futsala:'Futsala — PVE', btn_torneo:'Tournoi', btn_ranking:'Classement', btn_entrenamiento:'Entraînement', btn_deposito:'Dépôt', btn_retiro:'Retrait', btn_futcancha:'FutCancha — PVP', stamina:'Ballons d\'Endurance', pts:'Points', lvl:'Niveau', form:'Forme', overall:'Overall' },
    de: { tagline:'Das epischste NFT-Fußballspiel der Welt', btn_futbolistas:'Spieler', btn_market:'Marktplatz', btn_rewards:'Belohnungen', btn_daily:'Tägliche Belohnung', btn_logros:'Erfolge', btn_nivel:'Level', btn_futsala:'Futsala — PVE', btn_torneo:'Turnier', btn_ranking:'Rangliste', btn_entrenamiento:'Training', btn_deposito:'Einzahlung', btn_retiro:'Auszahlung', btn_futcancha:'FutCancha — PVP', stamina:'Ausdauerbälle', pts:'Punkte', lvl:'Level', form:'Form', overall:'Gesamtwert' },
    it: { tagline:'Il gioco di calcio NFT più epico del mondo', btn_futbolistas:'Giocatori', btn_market:'Mercato', btn_rewards:'Premi', btn_daily:'Premio Giornaliero', btn_logros:'Traguardi', btn_nivel:'Livello', btn_futsala:'Futsala — PVE', btn_torneo:'Torneo', btn_ranking:'Classifica', btn_entrenamiento:'Allenamento', btn_deposito:'Deposito', btn_retiro:'Prelievo', btn_futcancha:'FutCancha — PVP', stamina:'Palloni Stamina', pts:'Punti', lvl:'Livello', form:'Forma', overall:'Overall' },
    ja: { tagline:'世界最高のNFTサッカーゲーム', btn_futbolistas:'選手', btn_market:'マーケット', btn_rewards:'報酬', btn_daily:'デイリー報酬', btn_logros:'実績', btn_nivel:'レベル', btn_futsala:'フットサル — PVE', btn_torneo:'トーナメント', btn_ranking:'ランキング', btn_entrenamiento:'トレーニング', btn_deposito:'入金', btn_retiro:'出金', btn_futcancha:'フットカンチャ — PVP', stamina:'スタミナボール', pts:'ポイント', lvl:'レベル', form:'コンディション', overall:'総合値' },
    sv: { tagline:'Världens mest episka NFT-fotbollsspel', btn_futbolistas:'Spelare', btn_market:'Marknadsplats', btn_rewards:'Belöningar', btn_daily:'Daglig Belöning', btn_logros:'Prestationer', btn_nivel:'Nivå', btn_futsala:'Futsala — PVE', btn_torneo:'Turnering', btn_ranking:'Ranking', btn_entrenamiento:'Träning', btn_deposito:'Insättning', btn_retiro:'Uttag', btn_futcancha:'FutCancha — PVP', stamina:'Uthållighetsbollar', pts:'Poäng', lvl:'Nivå', form:'Form', overall:'Totalvärde' },
};
let currentLang = 'es';

function setLang(lang) {
    currentLang = lang;
    document.querySelectorAll('.lang-btn').forEach(b => b.classList.remove('active'));
    document.querySelector(`.lang-btn[onclick="setLang('${lang}')"]`)?.classList.add('active');
    const t = TRANSLATIONS[lang] || TRANSLATIONS.es;
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (t[key]) el.textContent = t[key];
    });
}

// ── FONDO ANIMADO ────────────────────────────────────────────
const FLAGS_EMOJIS = ['🇧🇷','🇦🇷','🇵🇹','🇬🇧','🇫🇷','🇩🇪','🇪🇸','🇺🇸','🇲🇽','🇯🇵','🇦🇺','🇸🇪','🇨🇴'];
function buildFlagsBg() {
    const bg = document.getElementById('flags-bg');
    FLAGS_EMOJIS.forEach((flag, i) => {
        const el = document.createElement('div');
        el.className = 'flag-emoji';
        el.textContent = flag;
        el.style.cssText = `left:${(i * 7.5 + Math.random()*5)}%;animation-duration:${18+i*3}s;animation-delay:${-i*2.5}s;`;
        bg.appendChild(el);
        // soccer balls
        const ball = document.createElement('div');
        ball.className = 'ball-bg';
        ball.textContent = '⚽';
        ball.style.cssText = `left:${(i * 7 + 3 + Math.random()*5)}%;animation-duration:${12+i*2}s;animation-delay:${-i*1.8}s;`;
        bg.appendChild(ball);
    });
}
buildFlagsBg();

// ── ESTADO ───────────────────────────────────────────────────
let gemas   = 3900;
let hp      = 0;
let puntaje = 0;
let nivel   = 1;
let statsPartidas = 0; // puntos de estadísticas acumulados (2 pts por partida)
let carrosComprados = [];
let durabilidades = {};
let fuelCartuchos = [true, true, true, true];

const WALLET   = "UQCObIMiCLIWIdT9FVPZWz5jAWocxqhu6bYzwtJzEkkHAsaY";
const IMG_BASE = "https://raw.githubusercontent.com/squadgamernft-star/TurboGarage/main/img/";
const IMG_FALLBACK = `data:image/svg+xml,${encodeURIComponent('<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 500"><rect fill="#0a130a" width="400" height="500"/><text x="200" y="280" text-anchor="middle" font-size="100" fill="#2d7a1f">⚽</text></svg>')}`;

// ── CATÁLOGO FUTBOLISTAS NFT ─────────────────────────────────
// Generamos imagenes Kawaii-Chibi en SVG para cada jugador
const PLAYERS = [
    { nombre:"Neymar Jr.",   pais:"Brasil",         flag:"🇧🇷", img:"player_320.svg",   precio:320,   hp:82,  pos:"Delantero", color1:"#009c3b", color2:"#002776" },
    { nombre:"James Rodríguez", pais:"Colombia",    flag:"🇨🇴", img:"player_800.svg",   precio:800,   hp:88,  pos:"Mediocampista", color1:"#fcd116", color2:"#003087" },
    { nombre:"Lionel Messi", pais:"Argentina",      flag:"🇦🇷", img:"player_1120.svg",  precio:1120,  hp:93,  pos:"Delantero", color1:"#74acdf", color2:"#ffffff" },
    { nombre:"Cristiano R.", pais:"Portugal",       flag:"🇵🇹", img:"player_1140.svg",  precio:1140,  hp:92,  pos:"Delantero", color1:"#006600", color2:"#ff0000" },
    { nombre:"Harry Kane",   pais:"Inglaterra",     flag:"🇬🇧", img:"player_1760.svg",  precio:1760,  hp:90,  pos:"Delantero", color1:"#ffffff", color2:"#cf142b" },
    { nombre:"Mbappé",       pais:"Francia",        flag:"🇫🇷", img:"player_2080.svg",  precio:2080,  hp:94,  pos:"Extremo",   color1:"#002395", color2:"#ed2939" },
    { nombre:"T. Müller",    pais:"Alemania",       flag:"🇩🇪", img:"player_2720.svg",  precio:2720,  hp:89,  pos:"Mediocampista", color1:"#000000", color2:"#ffffff" },
    { nombre:"Pedri",        pais:"España",         flag:"🇪🇸", img:"player_3040.svg",  precio:3040,  hp:91,  pos:"Mediocampista", color1:"#c60b1e", color2:"#ffc400" },
    { nombre:"P. Pulisic",   pais:"Estados Unidos", flag:"🇺🇸", img:"player_3840.svg",  precio:3840,  hp:87,  pos:"Extremo",   color1:"#002868", color2:"#bf0a30" },
    { nombre:"H. Lozano",    pais:"México",         flag:"🇲🇽", img:"player_4800.svg",  precio:4800,  hp:86,  pos:"Extremo",   color1:"#006847", color2:"#ce1126" },
    { nombre:"Kubo",         pais:"Japón",          flag:"🇯🇵", img:"player_5200.svg",  precio:5200,  hp:88,  pos:"Mediocampista", color1:"#bc002d", color2:"#ffffff" },
    { nombre:"Awer Mabil",   pais:"Australia",      flag:"🇦🇺", img:"player_5800.svg",  precio:5800,  hp:84,  pos:"Extremo",   color1:"#ffcd00", color2:"#006b3f" },
    { nombre:"Z. Ibrahimović", pais:"Suecia",       flag:"🇸🇪", img:"player_6400.svg",  precio:6400,  hp:95,  pos:"Delantero", color1:"#006aa7", color2:"#fecc02" },
];

// ── GENERAR IMAGEN SVG KAWAII-CHIBI POR JUGADOR ───────────────
function generatePlayerSVG(player) {
    const { color1, color2, flag, pais, pos, hp } = player;

    // Country flag background colors mapping
    const countryBgs = {
        'Brasil':         ['#009c3b','#ffd700','#002776'],
        'Colombia':       ['#fcd116','#003087','#ce1126'],
        'Argentina':      ['#74acdf','#ffffff','#74acdf'],
        'Portugal':       ['#006600','#ff0000','#ffffff'],
        'Inglaterra':     ['#cf142b','#ffffff','#003399'],
        'Francia':        ['#002395','#ffffff','#ed2939'],
        'Alemania':       ['#000000','#dd0000','#ffce00'],
        'España':         ['#c60b1e','#ffc400','#c60b1e'],
        'Estados Unidos': ['#002868','#bf0a30','#ffffff'],
        'México':         ['#006847','#ffffff','#ce1126'],
        'Japón':          ['#bc002d','#ffffff','#bc002d'],
        'Australia':      ['#00008b','#ffcd00','#006b3f'],
        'Suecia':         ['#006aa7','#fecc02','#006aa7'],
    };
    const [bg1, bg2, bg3] = countryBgs[pais] || ['#1a1a1a','#333','#555'];

    return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 380" width="300" height="380">
  <defs>
    <radialGradient id="bgGrad" cx="50%" cy="50%" r="70%">
      <stop offset="0%" stop-color="${bg2}" stop-opacity="0.9"/>
      <stop offset="60%" stop-color="${bg1}" stop-opacity="1"/>
      <stop offset="100%" stop-color="${bg3}" stop-opacity="1"/>
    </radialGradient>
    <filter id="glow"><feGaussianBlur stdDeviation="3" result="blur"/><feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%"><feDropShadow dx="0" dy="4" stdDeviation="6" flood-color="rgba(0,0,0,0.6)"/></filter>
  </defs>

  <!-- Background flag-themed gradient -->
  <rect width="300" height="380" fill="url(#bgGrad)" rx="16"/>
  <!-- Diagonal stripes -->
  <line x1="0" y1="100" x2="300" y2="0" stroke="${bg2}" stroke-width="40" opacity="0.15"/>
  <line x1="0" y1="200" x2="300" y2="80" stroke="${bg3}" stroke-width="30" opacity="0.12"/>
  <line x1="0" y1="320" x2="300" y2="200" stroke="${bg2}" stroke-width="25" opacity="0.1"/>
  <!-- Glow circle behind player -->
  <circle cx="150" cy="185" r="105" fill="${color1}" opacity="0.18" filter="url(#glow)"/>

  <!-- === KAWAII CHIBI PLAYER (3/4 angle) === -->
  <!-- Shadow on ground -->
  <ellipse cx="155" cy="355" rx="55" ry="10" fill="rgba(0,0,0,0.3)"/>

  <!-- Legs & Cleats -->
  <!-- Left leg (with ball) -->
  <rect x="118" y="290" width="22" height="50" rx="10" fill="${color2}" filter="url(#shadow)"/>
  <rect x="108" y="330" width="28" height="18" rx="7" fill="#111"/>
  <rect x="106" y="340" width="32" height="8" rx="4" fill="#333"/>
  <!-- Right leg -->
  <rect x="152" y="285" width="22" height="55" rx="10" fill="${color2}" filter="url(#shadow)"/>
  <rect x="148" y="332" width="28" height="18" rx="7" fill="#111"/>
  <rect x="146" y="342" width="32" height="8" rx="4" fill="#333"/>

  <!-- Shorts -->
  <path d="M112 265 Q130 290 148 265 L158 265 Q176 290 178 265 L185 240 Q150 255 115 240 Z" fill="${color1}" filter="url(#shadow)"/>

  <!-- Jersey body (3/4 view) -->
  <path d="M115 170 Q95 175 90 200 L100 240 Q150 255 200 240 L208 200 Q204 175 185 170 Z" fill="${color1}" filter="url(#shadow)"/>
  <!-- Jersey stripes -->
  <path d="M140 170 L137 250 L148 250 L150 170Z" fill="${color2}" opacity="0.6"/>
  <path d="M158 170 L155 250 L166 250 L168 170Z" fill="${color2}" opacity="0.6"/>
  <!-- Collar -->
  <path d="M138 170 Q150 162 162 170 L162 180 Q150 175 138 180Z" fill="${color2}"/>
  <!-- Arms -->
  <path d="M115 170 Q90 185 88 210 L100 215 Q110 195 118 185Z" fill="${color1}"/>
  <path d="M185 170 Q210 185 210 210 L198 215 Q190 195 182 185Z" fill="${color1}"/>
  <!-- Hands -->
  <circle cx="94" cy="215" r="10" fill="#FDBCB4"/>
  <circle cx="205" cy="215" r="10" fill="#FDBCB4"/>
  <!-- Number on jersey -->
  <text x="151" y="218" text-anchor="middle" font-family="'Orbitron',monospace" font-size="22" font-weight="bold" fill="${color2}" opacity="0.9">${Math.floor(hp/10)}</text>

  <!-- Neck -->
  <rect x="142" y="153" width="16" height="20" rx="7" fill="#FDBCB4"/>

  <!-- Head (big chibi) -->
  <ellipse cx="150" cy="128" rx="52" ry="50" fill="#FDBCB4" filter="url(#shadow)"/>
  <!-- Ear -->
  <circle cx="100" cy="132" r="11" fill="#F0A898"/>
  <circle cx="100" cy="132" r="6" fill="#FDBCB4"/>
  <circle cx="198" cy="132" r="11" fill="#F0A898"/>
  <circle cx="198" cy="132" r="6" fill="#FDBCB4"/>
  <!-- Hair (country-colored) -->
  <ellipse cx="150" cy="90" rx="52" ry="28" fill="${color1}"/>
  <rect x="98" y="80" width="104" height="30" rx="5" fill="${color1}"/>
  <!-- Hair side -->
  <rect x="98" y="88" width="14" height="45" rx="7" fill="${color1}"/>
  <!-- Eyes (big kawaii) -->
  <!-- Left eye -->
  <ellipse cx="130" cy="128" rx="14" ry="16" fill="white"/>
  <circle cx="130" cy="130" r="10" fill="#2d4a7a"/>
  <circle cx="130" cy="128" r="6" fill="#111"/>
  <circle cx="126" cy="124" r="3" fill="white"/>
  <!-- Right eye -->
  <ellipse cx="168" cy="128" rx="14" ry="16" fill="white"/>
  <circle cx="168" cy="130" r="10" fill="#2d4a7a"/>
  <circle cx="168" cy="128" r="6" fill="#111"/>
  <circle cx="164" cy="124" r="3" fill="white"/>
  <!-- Eyebrows -->
  <path d="M118 112 Q130 107 140 112" stroke="#6b4226" stroke-width="3" fill="none" stroke-linecap="round"/>
  <path d="M158 112 Q168 107 180 112" stroke="#6b4226" stroke-width="3" fill="none" stroke-linecap="round"/>
  <!-- Smile -->
  <path d="M136 146 Q150 158 164 146" stroke="#cc6666" stroke-width="3" fill="none" stroke-linecap="round"/>
  <!-- Cheek blush -->
  <circle cx="118" cy="144" r="9" fill="rgba(255,180,180,0.5)"/>
  <circle cx="182" cy="144" r="9" fill="rgba(255,180,180,0.5)"/>
  <!-- Chin detail -->
  <ellipse cx="150" cy="168" rx="20" ry="7" fill="#F0A898"/>

  <!-- SOCCER BALL on left foot -->
  <circle cx="112" cy="348" r="18" fill="white" filter="url(#shadow)"/>
  <path d="M112 330 l5 8 l-5 8 l-8 0 l-5 -8 l5 -8 z" fill="#222"/>
  <circle cx="112" cy="348" r="18" fill="none" stroke="#222" stroke-width="1.5"/>
  <circle cx="104" cy="342" r="4" fill="#333"/>
  <circle cx="120" cy="342" r="4" fill="#333"/>
  <circle cx="112" cy="354" r="4" fill="#333"/>

  <!-- Country flag badge -->
  <rect x="10" y="10" width="40" height="28" rx="6" fill="rgba(0,0,0,0.6)"/>
  <text x="30" y="30" text-anchor="middle" font-size="18">${flag}</text>

  <!-- Position badge -->
  <rect x="210" y="10" width="80" height="22" rx="11" fill="rgba(0,0,0,0.6)" stroke="${color2}" stroke-width="1"/>
  <text x="250" y="25" text-anchor="middle" font-family="'Rajdhani',sans-serif" font-size="11" fill="${color2}" font-weight="bold">${pos.toUpperCase()}</text>

  <!-- Overall badge -->
  <rect x="230" y="335" width="55" height="32" rx="10" fill="rgba(0,0,0,0.75)" stroke="#ffd700" stroke-width="1.5"/>
  <text x="257" y="349" text-anchor="middle" font-family="'Orbitron',monospace" font-size="9" fill="#ffd700">OVR</text>
  <text x="257" y="362" text-anchor="middle" font-family="'Orbitron',monospace" font-size="14" font-weight="bold" fill="#ffd700">${hp}</text>

  <!-- NFT shimmer overlay -->
  <rect width="300" height="380" rx="16" fill="url(#bgGrad)" opacity="0.06"/>
</svg>`;
}

// ── FUNCIÓN PARA SRC DE IMAGEN ────────────────────────────────
function playerImgSrc(idx) {
    const player = PLAYERS[idx];
    const svgContent = generatePlayerSVG(player);
    return 'data:image/svg+xml;charset=utf-8,' + encodeURIComponent(svgContent);
}

// ── HELPERS ──────────────────────────────────────────────────
const $id = id => document.getElementById(id);
const statusMsg = $id('status-msg');

function generatePlayerId(idx) {
    return 'FM-' + String(2025000 + idx * 137 + 43).padStart(8, '0');
}

function mostrarMensaje(texto, color = '#888') {
    statusMsg.textContent = texto;
    statusMsg.style.color = color;
    statusMsg.style.textShadow = `0 0 10px ${color}55`;
    clearTimeout(mostrarMensaje._t);
    mostrarMensaje._t = setTimeout(() => { statusMsg.textContent = ''; }, 5000);
}

function actualizarUI() {
    $id('gemas-stat').textContent      = gemas.toFixed(1);
    $id('hp-stat').textContent         = hp;
    $id('puntaje').textContent         = puntaje;
    $id('nivel-stat').textContent      = nivel;
    $id('modal-nivel-num').textContent = nivel;
    const md = $id('market-gemas-display');
    if (md) md.textContent = gemas.toFixed(1) + ' 💎';
    const mr = $id('my-rank-score');
    if (mr) mr.textContent = puntaje + ' pts';
    const cpd = $id('current-pts-display');
    if (cpd) cpd.textContent = statsPartidas;
    actualizarGasolina();
    actualizarFuelUI();
}

function actualizarGasolina() {
    const llenos = fuelCartuchos.filter(Boolean).length;
    const pct    = Math.round((llenos / 4) * 100);
    $id('gasolina').textContent = pct + '%';
}

// ── DURABILIDAD ──────────────────────────────────────────────
function getDurabilidad(idx) {
    if (durabilidades[idx] === undefined) durabilidades[idx] = 100;
    return parseFloat(durabilidades[idx].toFixed(1));
}
function colorDurabilidad(pct) {
    if (pct >= 60) return 'dur-high';
    if (pct >= 30) return 'dur-med';
    return 'dur-low';
}
function reducirDurabilidad(idx) {
    if (durabilidades[idx] === undefined) durabilidades[idx] = 100;
    durabilidades[idx] = Math.max(0, durabilidades[idx] - 0.8);
    saveDurabilidades();
}
function repararDurabilidad(idx) {
    durabilidades[idx] = 100;
    saveDurabilidades();
}
function saveDurabilidades() { localStorage.setItem('tg_durabilidades', JSON.stringify(durabilidades)); }
function loadDurabilidades() {
    const s = localStorage.getItem('tg_durabilidades');
    if (s) durabilidades = JSON.parse(s);
}

// ── CARTUCHOS ────────────────────────────────────────────────
function actualizarFuelUI() {
    for (let i = 0; i < 4; i++) {
        const el = $id(`fuel-${i}`);
        if (!el) continue;
        el.className = 'fuel-cartridge ' + (fuelCartuchos[i] ? 'full' : 'empty');
        el.textContent = fuelCartuchos[i] ? '⚽' : '';
        el.title = `Balón ${i+1} — ${fuelCartuchos[i] ? 'Disponible' : 'Agotado'}`;
    }
}

function usarCombustible(idx) {
    if (!fuelCartuchos[idx]) { mostrarMensaje(`⚠️ Balón ${idx+1} agotado. ¡Necesitas entrenamiento!`, '#ffaa00'); return; }
    if (carrosComprados.length === 0) { mostrarMensaje('⚠️ ¡Ficha un futbolista en el Marketplace!', '#ffaa00'); return; }
    const carroActivo = carrosComprados[0];
    fuelCartuchos[idx] = false;
    reducirDurabilidad(carroActivo);
    const dur = getDurabilidad(carroActivo);
    actualizarUI();
    let msg = `⚽ Balón ${idx+1} usado en partido. Forma física: ${dur}%`;
    let color = '#00ff88';
    if (dur < 30)  { msg += ' ⚠️ ¡Forma crítica!'; color = '#ff4444'; }
    else if (dur < 60) { msg += ' — Considera entrenar pronto'; color = '#ffaa00'; }
    mostrarMensaje(msg, color);
    saveFuelState();
}

function saveFuelState() { localStorage.setItem('tg_fuel', JSON.stringify(fuelCartuchos)); }
function loadFuelState() {
    const s = localStorage.getItem('tg_fuel');
    if (s) fuelCartuchos = JSON.parse(s);
}

// ── MODALES ──────────────────────────────────────────────────
function openModal(id) {
    const el = $id(id);
    if (!el) return;
    el.classList.add('active');
    document.body.style.overflow = 'hidden';
    if (id === 'modal-marketplace')    { actualizarUI(); buildMarket(); }
    if (id === 'modal-recompensa')     checkRewardStatus();
    if (id === 'modal-recompensa-pts') buildRewardPts();
    if (id === 'modal-garage')         buildGarage();
    if (id === 'modal-mantenimiento')  buildMantenimiento();
    if (id === 'modal-logros')         buildLogros();
}

function closeModal(id) {
    const el = $id(id);
    if (!el) return;
    el.classList.remove('active');
    document.body.style.overflow = '';
}

document.querySelectorAll('.modal-overlay').forEach(ov => {
    ov.addEventListener('click', e => { if (e.target === ov) closeModal(ov.id); });
});

function switchTab(e, group, panel) {
    const modal = $id(`modal-${group}`);
    if (!modal) return;
    modal.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
    modal.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
    e.currentTarget.classList.add('active');
    const panelEl = $id(`${group}-${panel}`);
    if (panelEl) panelEl.classList.add('active');
}

// ── PLAYER DETAIL MODAL ──────────────────────────────────────
function gemasXDia(c) { return parseFloat((c.hp / 18).toFixed(1)); }

function openCarDetail(idx, fromGarage) {
    const c       = PLAYERS[idx];
    const dur     = getDurabilidad(idx);
    const durCls  = colorDurabilidad(dur);
    const fuelLlenos = fuelCartuchos.filter(Boolean).length;
    const gpd     = gemasXDia(c);
    const playerId = generatePlayerId(idx);

    $id('car-detail-img').src = playerImgSrc(idx);
    $id('car-detail-name').textContent = c.nombre;
    $id('car-detail-country-flag').textContent = c.flag + ' ' + c.pais;
    $id('car-detail-player-id').textContent = 'ID: ' + playerId;
    $id('car-detail-hp').textContent   = c.hp + ' OVR';
    $id('car-detail-dur').textContent  = dur + '%';
    $id('car-detail-dur').style.color  = dur >= 60 ? 'var(--green)' : dur >= 30 ? 'var(--gold)' : 'var(--red)';
    $id('car-detail-fuel').textContent = fuelLlenos + ' / 4';
    $id('car-detail-earn').textContent = gpd + ' 💎';

    // Extra stats
    const spd = Math.round(c.hp * 0.9 + Math.random()*5 - 2);
    const def = Math.round(c.hp * 0.6 + Math.random()*8);
    const phy = Math.round(c.hp * 0.75 + Math.random()*6);
    $id('car-detail-extra-stats').innerHTML = `
        <div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:10px;padding:10px;text-align:center">
            <div style="font-size:1.2em">⚡</div><div style="font-size:0.68em;color:#888;text-transform:uppercase">Velocidad</div>
            <div style="font-family:'Orbitron',sans-serif;font-size:0.95em;color:var(--cyan);font-weight:700">${spd}</div>
        </div>
        <div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:10px;padding:10px;text-align:center">
            <div style="font-size:1.2em">🛡️</div><div style="font-size:0.68em;color:#888;text-transform:uppercase">Defensa</div>
            <div style="font-family:'Orbitron',sans-serif;font-size:0.95em;color:var(--orange);font-weight:700">${def}</div>
        </div>
        <div style="background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:10px;padding:10px;text-align:center">
            <div style="font-size:1.2em">💪</div><div style="font-size:0.68em;color:#888;text-transform:uppercase">Físico</div>
            <div style="font-family:'Orbitron',sans-serif;font-size:0.95em;color:var(--purple);font-weight:700">${phy}</div>
        </div>`;

    // Durability bar
    $id('car-detail-dur-bar').innerHTML = `
        <div style="display:flex;justify-content:space-between;font-size:0.78em;color:#888;margin-bottom:5px">
            <span>💪 Forma Física del Jugador</span>
            <span style="color:${dur>=60?'var(--green)':dur>=30?'var(--gold)':'var(--red)'}">${dur}%</span>
        </div>
        <div style="background:rgba(255,255,255,0.07);border-radius:50px;height:8px;overflow:hidden">
            <div class="dur-bar-fill ${durCls}" style="width:${dur}%;height:100%;border-radius:50px;transition:width 0.5s"></div>
        </div>
        <div style="font-size:0.72em;color:#555;margin-top:4px;text-align:center">📉 −0.8% por partido jugado · Recupera con entrenamiento</div>`;

    // Fuel/ball dots
    let dots = '';
    for (let i = 0; i < 4; i++) {
        const lleno = fuelCartuchos[i];
        dots += `<div style="width:36px;height:36px;border-radius:50%;border:2px solid ${lleno?'var(--green)':'rgba(255,255,255,0.1)'};background:${lleno?'radial-gradient(circle at 35% 35%, #fff 0%, #444 60%, #000 100%)':'rgba(255,255,255,0.03)'};display:flex;align-items:center;justify-content:center;font-size:1.3em;box-shadow:${lleno?'0 0 10px rgba(0,255,136,0.4)':'none'}">${lleno?'⚽':''}</div>`;
    }
    $id('car-detail-fuel-dots').innerHTML = dots;

    // ID badge
    const idHtml = `<div style="background:rgba(0,212,255,0.07);border:1px solid rgba(0,212,255,0.2);border-radius:8px;padding:8px 14px;margin-bottom:12px;font-size:0.8em;color:#888">
        🆔 ID del Jugador: <strong style="color:var(--cyan);font-family:'Orbitron',sans-serif">${playerId}</strong>
    </div>`;

    if (fromGarage) {
        $id('car-detail-action-btn').innerHTML = idHtml + `<button class="primary-btn" style="font-size:0.82em" onclick="closeCarDetail();openModal('modal-mantenimiento')">🏋️ Ir a Entrenamiento</button>`;
    } else {
        const comprado = carrosComprados.includes(idx);
        $id('car-detail-action-btn').innerHTML = idHtml + (comprado
            ? `<button class="primary-btn" style="font-size:0.82em;opacity:0.5;cursor:default">✅ Ya está en tu Plantilla</button>`
            : `<button class="primary-btn" style="font-size:0.82em" onclick="closeCarDetail();comprarCarro(${idx})">💎 Fichar por ${c.precio.toLocaleString()} gemas</button>`);
    }

    $id('modal-car-detail').classList.add('active');
    document.body.style.overflow = 'hidden';
}

function closeCarDetail() {
    $id('modal-car-detail').classList.remove('active');
    document.body.style.overflow = '';
}
$id('modal-car-detail').addEventListener('click', e => {
    if (e.target === $id('modal-car-detail')) closeCarDetail();
});

// ── MARKETPLACE ──────────────────────────────────────────────
function buildMarket() {
    $id('car-market-grid').innerHTML = PLAYERS.map((c, i) => {
        const comprado = carrosComprados.includes(i);
        const gpd = gemasXDia(c);
        return `<div class="car-card" id="card-${i}">
            <div class="car-card-img-wrap" onclick="openCarDetail(${i}, false)">
                <img class="car-card-img" src="${playerImgSrc(i)}" alt="${c.nombre}">
            </div>
            <div class="car-card-info">
                <div class="car-card-name">${c.nombre}</div>
                <div class="car-card-country">${c.flag} ${c.pais}</div>
                <div style="font-size:0.74em;color:#888;margin-bottom:2px">⚡ OVR ${c.hp} &nbsp;·&nbsp; <span style="color:var(--gold)">💎 ${gpd}/día</span></div>
                <div class="car-card-price">💎 ${c.precio.toLocaleString()}</div>
                <button class="buy-car-btn" id="buy-btn-${i}" onclick="comprarCarro(${i})" ${comprado?'disabled':''}>
                    ${comprado ? '✅ Fichado' : '⚽ Fichar'}
                </button>
            </div>
        </div>`;
    }).join('');
}

function comprarCarro(idx) {
    const c = PLAYERS[idx];
    if (carrosComprados.includes(idx)) { mostrarMensaje('⚠️ Ya fichaste este jugador','#ffaa00'); return; }
    if (gemas < c.precio) { mostrarMensaje(`❌ Faltan ${(c.precio - gemas).toFixed(1)} 💎 — Ve a "Depósito de Gemas"`,'#ff4444'); return; }
    gemas -= c.precio;
    hp    += c.hp;
    puntaje += Math.round(c.precio / 10);
    carrosComprados.push(idx);
    durabilidades[idx] = 100;
    saveDurabilidades();
    actualizarUI();
    if (carrosComprados.length === 1) {
        $id('main-car-img').src = playerImgSrc(idx);
    }
    const btn = $id(`buy-btn-${idx}`);
    if (btn) { btn.textContent = '✅ Fichado'; btn.disabled = true; }
    buildLogros();
    setTimeout(() => openCarDetail(idx, true), 400);
    mostrarMensaje(`✅ ¡${c.nombre} fichado! +${c.hp} OVR`,'#00ff88');
}
buildMarket();

// ── RECOMPENSAS POR ESTADÍSTICAS (PARTIDAS) ──────────────────
const REWARD_SECTIONS = [
    { icon:'⚽', key:'penales',    label:'Penales',     desc:'Ejecuta penales en Futsala', pts_req:100, reward_gems:1, level_req:1 },
    { icon:'🎯', key:'pases',      label:'Pases',       desc:'Acumula asistencias en partidas', pts_req:100, reward_gems:2, level_req:1 },
    { icon:'🦵', key:'tiros',      label:'Tiros Libres',desc:'Marca de tiro libre en FutCancha', pts_req:100, reward_gems:2, level_req:2 },
    { icon:'🏃', key:'esquinas',   label:'Córners',     desc:'Ejecuta tiros de esquina', pts_req:100, reward_gems:3, level_req:2 },
    { icon:'🥅', key:'goles',      label:'Goles',       desc:'Anota goles consecutivos', pts_req:100, reward_gems:3, level_req:3 },
    { icon:'🏆', key:'victorias',  label:'Victorias',   desc:'Gana partidas en cualquier modo', pts_req:100, reward_gems:4, level_req:3 },
    { icon:'🌟', key:'hattrick',   label:'Hat-tricks',  desc:'Logra 3 goles en un mismo partido', pts_req:100, reward_gems:5, level_req:5 },
    { icon:'👑', key:'maestro',    label:'Maestro',     desc:'Domina todas las estadísticas', pts_req:100, reward_gems:5, level_req:10 },
];

function buildRewardPts() {
    actualizarUI();
    const claimedSections = JSON.parse(localStorage.getItem('tg_claimed_sections') || '{}');
    const pct = Math.min(100, Math.round((statsPartidas / 100) * 100));

    let html = '';
    REWARD_SECTIONS.forEach(s => {
        const claimed = !!claimedSections[s.key];
        const canClaim = statsPartidas >= s.pts_req && nivel >= s.level_req && !claimed;
        const lockedLevel = nivel < s.level_req;
        html += `<div class="reward-section">
            <h4>${s.icon} ${s.label}</h4>
            <div class="reward-stat-row">
                <div class="reward-stat-label">${s.desc}</div>
                <div class="reward-stat-value">${claimed ? '✅ Reclamado' : `+${s.reward_gems} 💎`}</div>
            </div>
            <div style="font-size:0.76em;color:#888;margin-bottom:8px">
                Nivel requerido: <span style="color:${nivel >= s.level_req ? 'var(--green)' : '#888'}">${s.level_req}</span> &nbsp;·&nbsp;
                Pts requeridos: <span style="color:${statsPartidas >= s.pts_req ? 'var(--green)' : '#888'}">${s.pts_req}</span>
            </div>
            <div class="pts-bar-bg">
                <div class="pts-bar-fill" style="width:${pct}%"></div>
            </div>
            <div style="display:flex;justify-content:space-between;font-size:0.72em;color:#555;margin-top:3px">
                <span>${statsPartidas} / 100 pts</span>
                <span>~${Math.max(0, Math.ceil((100 - statsPartidas)/2))} partidas restantes</span>
            </div>
            ${!claimed ? `<button style="width:100%;margin-top:10px;padding:9px;background:${canClaim?'rgba(0,255,136,0.12)':'rgba(255,255,255,0.04)'};border:1px solid ${canClaim?'var(--green)':'rgba(255,255,255,0.1)'};border-radius:8px;color:${canClaim?'var(--green)':'#555'};font-family:'Rajdhani',sans-serif;font-weight:700;font-size:0.84em;cursor:${canClaim?'pointer':'not-allowed'};text-transform:uppercase;transition:all 0.3s"
                onclick="${canClaim?`claimSection('${s.key}', ${s.reward_gems})`:'void(0)'}">
                ${lockedLevel ? '🔒 Nivel '+s.level_req+' requerido' : canClaim ? '✅ Reclamar +'+s.reward_gems+' 💎' : '⏳ Juega más partidas'}
            </button>` : ''}
        </div>`;
    });
    $id('reward-pts-sections').innerHTML = html;
}

function claimSection(key, gems) {
    const claimedSections = JSON.parse(localStorage.getItem('tg_claimed_sections') || '{}');
    if (claimedSections[key]) { mostrarMensaje('⚠️ Ya reclamado','#ffaa00'); return; }
    gemas += gems;
    claimedSections[key] = true;
    localStorage.setItem('tg_claimed_sections', JSON.stringify(claimedSections));
    actualizarUI();
    buildRewardPts();
    mostrarMensaje(`✅ +${gems} 💎 reclamadas por ${key}!`, '#00ff88');
}

// ── RECOMPENSA DIARIA 30 DIAS ─────────────────────────────────
const DAILY_ICONS_ODD  = ['⚽','🥅','⛹️','🏃','🦵','🎯','🏋️','🤸'];
const DAILY_ICONS_EVEN = ['🪙','🥇','💛','🏅','🎖️','🌟'];

function buildRewardDays() {
    const diaActual = parseInt(localStorage.getItem('tg_reward_day') || '1');
    let html = '';
    for (let d = 1; d <= 30; d++) {
        const claimed = d < diaActual;
        const isToday = d === diaActual;
        const isGold  = d % 2 === 0 && d !== 30;
        const isDay30 = d === 30;
        let cls = 'reward-day';
        if (isDay30) cls += ' special'; else if (isGold) cls += ' gold-day';
        if (claimed) cls += ' claimed';
        if (isToday) cls += ' today';
        const icon  = isDay30 ? '👑' : isGold ? DAILY_ICONS_EVEN[d%DAILY_ICONS_EVEN.length] : DAILY_ICONS_ODD[d%DAILY_ICONS_ODD.length];
        const label = isDay30 ? '25💎' : isGold ? '🪙' : '1💎';
        html += `<div class="${cls}"><span class="day-icon">${icon}</span><div style="font-weight:700">${d}</div><small>${label}</small></div>`;
    }
    $id('reward-days-grid').innerHTML = html;
}

function checkRewardStatus() {
    buildRewardDays();
    const hoy = new Date().toDateString();
    const claimed = localStorage.getItem('tg_last_reward_date') === hoy;
    const btn = $id('btn-claim-reward');
    if (btn) { btn.disabled = claimed; btn.textContent = claimed ? '✅ Ya reclamada hoy' : '✅ Reclamar Recompensa'; }
}

function claimReward() {
    const hoy = new Date().toDateString();
    if (localStorage.getItem('tg_last_reward_date') === hoy) { mostrarMensaje('⚠️ Ya reclamaste hoy','#ffaa00'); return; }
    const diaActual = parseInt(localStorage.getItem('tg_reward_day') || '1');
    const isDay30   = diaActual === 30;
    const isGold    = diaActual % 2 === 0 && !isDay30;
    let msg = '';
    if (isDay30)     { gemas += 25; msg = '👑 ¡Día 30! +25 💎'; }
    else if (isGold) { msg = '🪙 ¡Moneda de oro reclamada!'; }
    else             { gemas += 1; msg = '⚽ +1 💎 reclamada'; }
    const sig = diaActual < 30 ? diaActual + 1 : 1;
    localStorage.setItem('tg_reward_day', String(sig));
    localStorage.setItem('tg_last_reward_date', hoy);
    puntaje += 1;
    actualizarUI();
    buildRewardDays();
    const btn = $id('btn-claim-reward');
    if (btn) { btn.disabled = true; btn.textContent = '✅ Ya reclamada hoy'; }
    mostrarMensaje(msg, isDay30 ? '#a855f7' : isGold ? '#ffd700' : '#00ff88');
    setTimeout(() => closeModal('modal-recompensa'), 1800);
}

// ── LOGROS ───────────────────────────────────────────────────
const LOGROS = {
    jugadores: [
        { meta:1,  icon:'⚽', t:'Primera Ficha',    d:'Ficha tu primer jugador NFT' },
        { meta:3,  icon:'👕', t:'Pequeño Equipo',   d:'Ficha 3 jugadores' },
        { meta:5,  icon:'🏆', t:'Mi Plantilla',     d:'Ficha 5 jugadores' },
        { meta:10, icon:'🌟', t:'Seleccionador',    d:'Ficha 10 jugadores' },
        { meta:13, icon:'👑', t:'Plantilla Mundialista', d:'Ficha todos los jugadores' },
    ],
    partidas: [
        { meta:1,  icon:'🏁', t:'Debut',            d:'Juega tu primera partida' },
        { meta:5,  icon:'⚽', t:'Veterano',          d:'Juega 5 partidas' },
        { meta:10, icon:'🏃', t:'Habitual',          d:'Juega 10 partidas' },
        { meta:25, icon:'🏟️', t:'Profesional',      d:'Juega 25 partidas' },
        { meta:50, icon:'🏆', t:'Leyenda',           d:'Juega 50 partidas' },
    ],
    goles: [
        { meta:10,   icon:'🥅', t:'Primer Gol',      d:'Acumula 10 gemas' },
        { meta:100,  icon:'⚽', t:'Goleador',         d:'Acumula 100 gemas' },
        { meta:500,  icon:'🌟', t:'Pichichi',         d:'Acumula 500 gemas' },
        { meta:1000, icon:'🏆', t:'Rey del Gol',      d:'Acumula 1000 gemas' },
        { meta:5000, icon:'👑', t:'Balón de Oro',     d:'Acumula 5000 gemas' },
    ],
    racha: [
        { meta:3,  icon:'📅', t:'Racha Inicial',    d:'3 días seguidos' },
        { meta:7,  icon:'🔥', t:'Semana Completa',  d:'7 días seguidos' },
        { meta:14, icon:'⚡', t:'Quincenal',         d:'14 días seguidos' },
        { meta:21, icon:'🌟', t:'Mes en Marcha',    d:'21 días seguidos' },
        { meta:30, icon:'👑', t:'Mes Perfecto',     d:'30 días seguidos' },
    ],
};

function getProgreso(tipo, meta) {
    const map = {
        jugadores: carrosComprados.length,
        partidas:  Math.floor(statsPartidas / 2),
        goles:     Math.floor(gemas),
        racha:     Math.max(0, parseInt(localStorage.getItem('tg_reward_day') || '1') - 1),
    };
    return { actual: map[tipo] || 0, meta };
}

function buildLogrosList(tipo) {
    $id(`logros-${tipo}-list`).innerHTML = LOGROS[tipo].map(l => {
        const { actual, meta } = getProgreso(tipo, l.meta);
        const ok  = actual >= meta;
        const pct = Math.min(100, Math.round(actual / meta * 100));
        return `<div class="logro-item ${ok ? 'desbloqueado' : ''}">
            <span class="logro-icon">${l.icon}</span>
            <div class="logro-info">
                <h4>${l.t}</h4>
                <p>${l.d} — ${actual}/${meta}</p>
                <div class="logro-progress"><div class="logro-progress-fill" style="width:${pct}%"></div></div>
            </div>
            <div class="logro-reward">
                <span style="color:${ok ? 'var(--green)' : '#555'}">${ok ? '✅' : '🔒'}</span><br>
                <small style="color:var(--gold)">+2💎</small>
            </div>
        </div>`;
    }).join('');
}

function buildLogros() {
    ['jugadores','partidas','goles','racha'].forEach(buildLogrosList);
}

// ── GARAGE / PLANTILLA ────────────────────────────────────────
function buildGarage() {
    let html = '';
    for (let i = 0; i < 20; i++) {
        const idx = carrosComprados[i];
        if (idx !== undefined) {
            const c   = PLAYERS[idx];
            const dur = getDurabilidad(idx);
            const durCls = colorDurabilidad(dur);
            const durLabel = dur >= 60 ? '🟢 Buena' : dur >= 30 ? '🟡 Desgaste' : '🔴 Crítico';
            html += `<div class="garage-slot-card occupied" onclick="openCarDetail(${idx}, true)">
                <img src="${playerImgSrc(idx)}" alt="${c.nombre}"
                     style="width:100%;aspect-ratio:3/4;object-fit:cover;border-radius:7px;display:block;margin-bottom:6px;border:1px solid rgba(45,122,31,0.3)">
                <div class="gs-name">${c.nombre}</div>
                <div style="font-size:0.7em;color:#888;margin-top:2px">${c.flag} OVR ${c.hp}</div>
                <div class="dur-bar-wrap" style="padding:0 2px;margin-top:5px">
                    <div class="dur-bar-label">
                        <span style="font-size:0.62em">💪</span>
                        <span style="font-size:0.62em;color:${dur>=60?'var(--green)':dur>=30?'var(--gold)':'var(--red)'}">${dur}%</span>
                    </div>
                    <div class="dur-bar-bg"><div class="dur-bar-fill ${durCls}" style="width:${dur}%"></div></div>
                    <div style="font-size:0.58em;text-align:center;margin-top:2px;color:#555">${durLabel}</div>
                </div>
            </div>`;
        } else {
            html += `<div class="garage-slot-card"><div class="gs-icon">➕</div><div class="gs-name">Slot ${i+1}</div><div style="font-size:0.7em;color:#333;margin-top:2px">Vacío</div></div>`;
        }
    }
    $id('garage-grid-vehicles').innerHTML = html;
}

// ── ENTRENAMIENTO ─────────────────────────────────────────────
function buildMantenimiento() {
    const lista = $id('mant-list');
    if (!carrosComprados.length) {
        lista.innerHTML = `<div class="coming-soon-box">No tienes futbolistas. ¡Ficha uno en el <strong style="color:var(--green)">Marketplace</strong>!</div>`;
        return;
    }
    lista.innerHTML = carrosComprados.map(idx => {
        const c   = PLAYERS[idx];
        const dur = getDurabilidad(idx);
        const durClass = colorDurabilidad(dur);
        return `<div class="mant-item">
            <span class="mant-icon">${c.flag}</span>
            <div class="mant-info">
                <h4>${c.nombre}</h4>
                <p>⚡ OVR ${c.hp} · ${c.pos}</p>
                <div class="dur-bar-wrap" style="margin-top:6px">
                    <div class="dur-bar-label">
                        <span>💪 Forma Física</span>
                        <span style="color:${dur >= 60 ? 'var(--green)' : dur >= 30 ? 'var(--gold)' : 'var(--red)'}">${dur}%</span>
                    </div>
                    <div class="dur-bar-bg"><div class="dur-bar-fill ${durClass}" style="width:${dur}%"></div></div>
                    <div style="font-size:0.74em;color:#666;margin-top:3px">📉 -0.8% por partido jugado</div>
                </div>
            </div>
            <div class="mant-price">
                <strong>$${Math.ceil(c.precio/320)} USDT</strong>
                <small>Entrenamiento</small>
                <button class="mant-btn" onclick="pagarMantenimiento(${idx})">🏋️ Entrenar</button>
            </div>
        </div>`;
    }).join('');
}

async function pagarMantenimiento(idx) {
    const c = PLAYERS[idx];
    if (!window.tonConnectUI || !tonConnectUI.connected) { mostrarMensaje('⚠️ Conecta tu wallet primero','#ffaa00'); tonConnectUI.openModal(); return; }
    const costo = Math.ceil(c.precio/320);
    mostrarMensaje(`⏳ Procesando $${costo} USDT...`,'#00d4ff');
    const ton = (costo / 5).toFixed(4);
    const tx  = { validUntil: Math.floor(Date.now()/1000)+300, messages: [{ address: WALLET, amount: String(Math.round(parseFloat(ton)*1e9)) }] };
    try {
        await tonConnectUI.sendTransaction(tx);
        repararDurabilidad(idx);
        fuelCartuchos = [true, true, true, true];
        saveFuelState();
        actualizarUI();
        buildMantenimiento();
        mostrarMensaje(`✅ ${c.nombre} entrenado al 100% + estamina recargada`,'#00ff88');
        closeModal('modal-mantenimiento');
    } catch(e) { mostrarMensaje('❌ Transacción cancelada','#ff4444'); }
}

// ── FUTSALA PVE (vs IA) ───────────────────────────────────────
function iniciarFutsala(cancha) {
    if (carrosComprados.length === 0) { mostrarMensaje('⚠️ ¡Ficha un futbolista primero!','#ffaa00'); return; }

    const futsalaBody = $id('futsala-body');
    futsalaBody.innerHTML = `<div class="futsala-match-loading">
        <div class="pitch-loader">⚽</div>
        <div style="margin-top:14px;font-family:'Bebas Neue',sans-serif;font-size:1.4em;letter-spacing:3px;color:var(--green)">CARGANDO PARTIDO...</div>
        <div style="color:#888;font-size:0.84em;margin-top:6px">${cancha} vs IA</div>
    </div>`;

    setTimeout(() => {
        const playerIdx = carrosComprados[0];
        const player = PLAYERS[playerIdx];
        const playerId = generatePlayerId(playerIdx);

        // Simulación de partido vs IA
        const difficulty = { 'Cancha Barrial': 0.7, 'Estadio Municipal': 0.55, 'Copa del Mundo': 0.42, 'Noche de Gala': 0.35 };
        const winChance = difficulty[cancha] || 0.5;
        const playerGoals = Math.floor(Math.random() * 4);
        const iaGoals = Math.random() < winChance
            ? Math.max(0, playerGoals - 1 - Math.floor(Math.random()*2))
            : playerGoals + 1 + Math.floor(Math.random()*2);
        const playerWins = playerGoals > iaGoals;
        const draw = playerGoals === iaGoals;

        // +2 puntos de estadísticas por partida
        statsPartidas += 2;
        if (statsPartidas > 100) statsPartidas = 100;
        localStorage.setItem('tg_stats_partidas', statsPartidas);
        puntaje += playerWins ? 5 : draw ? 2 : 1;
        actualizarUI();

        // Generar eventos del partido
        const minutes = [8,14,23,31,38,45,52,59,67,72,81,88];
        const events = [];
        let pg = 0, ig = 0;
        const shuffled = minutes.sort(() => Math.random()-0.5);
        shuffled.slice(0, playerGoals + iaGoals + 3).forEach((min, i) => {
            if (pg < playerGoals) { events.push({min, type:'goal', text:`⚽ GOL — ${player.nombre} marca! (min ${min})`}); pg++; }
            else if (ig < iaGoals) { events.push({min, type:'ia', text:`🤖 Gol IA (min ${min})`}); ig++; }
            else { const r = Math.random(); events.push({min, type:'event', text: r<0.3?`🟨 Tarjeta amarilla IA (min ${min})`:r<0.6?`🎯 Tiro libre ${player.nombre} (min ${min})`:`🏃 Contraataque IA (min ${min})`}); }
        });
        events.sort((a,b)=>a.min-b.min);

        const resultColor = playerWins ? 'var(--green)' : draw ? 'var(--gold)' : 'var(--red)';
        const resultText  = playerWins ? '¡VICTORIA! 🏆' : draw ? '¡EMPATE!' : 'DERROTA';
        const ptsGained   = playerWins ? 5 : draw ? 2 : 1;

        futsalaBody.innerHTML = `
        <div style="margin-bottom:10px">
            <button style="background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.15);border-radius:8px;color:#aaa;padding:7px 14px;cursor:pointer;font-family:'Rajdhani',sans-serif;font-weight:600"
                onclick="resetFutsala()">← Volver a Canchas</button>
        </div>
        <div class="match-result-box">
            <div style="text-align:center;font-size:0.75em;color:#888;text-transform:uppercase;letter-spacing:2px;margin-bottom:8px">🏟️ ${cancha} — Futsala PVE</div>
            <div style="display:flex;align-items:center;justify-content:space-around;margin:10px 0;">
                <div style="text-align:center">
                    <img src="${playerImgSrc(playerIdx)}" style="width:70px;height:88px;object-fit:cover;border-radius:10px;border:2px solid var(--green);display:block;margin:0 auto 6px">
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:0.9em;letter-spacing:2px">${player.nombre}</div>
                    <div class="player-id-badge">${playerId}</div>
                    <div style="font-size:0.72em;color:#888;margin-top:3px">OVR ${player.hp}</div>
                </div>
                <div style="text-align:center">
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:4em;color:var(--gold);line-height:1">${playerGoals} - ${iaGoals}</div>
                    <div style="font-size:0.7em;color:#888;text-transform:uppercase;letter-spacing:2px;margin-top:4px">Final</div>
                </div>
                <div style="text-align:center">
                    <div style="font-size:3.5em;margin-bottom:6px">🤖</div>
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:0.9em;letter-spacing:2px">IA ${cancha}</div>
                    <div style="font-size:0.72em;color:#888;margin-top:3px">Dificultad</div>
                </div>
            </div>
            <div style="border-top:1px solid rgba(255,255,255,0.07);padding-top:10px;margin-top:8px">
                ${events.map(ev => `<div class="match-event ${ev.type==='goal'?'goal':''}">${ev.text}</div>`).join('')}
            </div>
            <div class="match-winner-banner" style="border:1px solid ${resultColor};background:rgba(0,0,0,0.3);margin-top:14px">
                <h4 style="color:${resultColor}">${resultText}</h4>
                <div style="font-size:0.82em;color:#888;margin-top:6px">+${ptsGained} pts de estadística · Total: <strong style="color:var(--green)">${statsPartidas}</strong>/100</div>
                <div style="font-size:0.78em;color:var(--gold);margin-top:4px">+${puntaje} puntos globales</div>
            </div>
        </div>`;
    }, 2200);
}

function resetFutsala() {
    const futsalaBody = $id('futsala-body');
    futsalaBody.innerHTML = `
        <div class="futsala-info-box">
            ✅ Modo GRATUITO — Sin gemas &nbsp;·&nbsp; 🤖 Vs Inteligencia Artificial &nbsp;·&nbsp; +2 pts estadísticas por partida
        </div>
        <p style="color:#888;font-size:0.84em;margin-bottom:12px;">Elige una cancha y compite contra la IA:</p>
        <div class="map-grid">
            <div class="map-card" onclick="iniciarFutsala('Cancha Barrial')">
                <div class="map-card-img" style="background:linear-gradient(135deg,#0a1a0a,#1a3a1a)">🏟️</div>
                <div class="map-card-info"><h4>Cancha Barrial</h4><p>Partidos rápidos en cancha local. IA nivel fácil. Ideal para comenzar.</p><span class="map-tag">⭐ Fácil · +2 pts</span></div>
            </div>
            <div class="map-card" onclick="iniciarFutsala('Estadio Municipal')">
                <div class="map-card-img" style="background:linear-gradient(135deg,#1a1a0a,#3a2a0a)">🏟️</div>
                <div class="map-card-info"><h4>Estadio Municipal</h4><p>IA de nivel medio. Tácticas variadas y mayor presión defensiva.</p><span class="map-tag">⭐⭐ Medio · +2 pts</span></div>
            </div>
            <div class="map-card" onclick="iniciarFutsala('Copa del Mundo')">
                <div class="map-card-img" style="background:linear-gradient(135deg,#1a0a0a,#3a0a0a)">🏆</div>
                <div class="map-card-info"><h4>Copa del Mundo</h4><p>IA élite. Alta dificultad. Más puntos de estadística al ganar.</p><span class="map-tag">⭐⭐⭐ Difícil · +2 pts</span></div>
            </div>
            <div class="map-card" onclick="iniciarFutsala('Noche de Gala')">
                <div class="map-card-img" style="background:linear-gradient(135deg,#0a0a1a,#0a1a3a)">🌃</div>
                <div class="map-card-info"><h4>Noche de Gala</h4><p>Estadio nocturno. IA con jugadas maestras. El reto máximo.</p><span class="map-tag">⭐⭐⭐⭐ Experto · +2 pts</span></div>
            </div>
        </div>`;
}

// ── FUTCANCHA PVP ─────────────────────────────────────────────
function buscarPartidaPVP(modo) {
    if (carrosComprados.length === 0) { mostrarMensaje('⚠️ ¡Ficha un futbolista primero!','#ffaa00'); return; }

    const pvpContent = $id('pvp-content');
    const playerIdx  = carrosComprados[0];
    const player     = PLAYERS[playerIdx];
    const playerId   = generatePlayerId(playerIdx);

    pvpContent.innerHTML = `<div class="pvp-search-box">
        <div class="pvp-search-icon">🔍</div>
        <div style="font-family:'Bebas Neue',sans-serif;font-size:1.4em;letter-spacing:3px;color:var(--green);margin-top:14px">BUSCANDO RIVAL...</div>
        <div style="color:#888;font-size:0.84em;margin-top:8px">Emparejando por OVR ${player.hp} · Modo ${modo}</div>
        <div style="margin-top:20px">
            <div class="pvp-player-vs">
                <div class="pvp-player-card">
                    <img src="${playerImgSrc(playerIdx)}" style="width:60px;height:75px;object-fit:cover;border-radius:8px;border:2px solid var(--green);display:block;margin:0 auto 8px">
                    <div style="font-weight:700;font-size:0.9em">${player.nombre}</div>
                    <div class="player-id-badge">${playerId}</div>
                    <div style="font-size:0.76em;color:#888;margin-top:4px">OVR ${player.hp}</div>
                </div>
                <div class="pvp-vs">VS</div>
                <div class="pvp-player-card">
                    <div style="font-size:2.5em;margin-bottom:8px">❓</div>
                    <div style="font-weight:700;font-size:0.9em;color:#888">Buscando...</div>
                    <div style="font-size:0.76em;color:#555;margin-top:4px">OVR ~${player.hp}</div>
                </div>
            </div>
        </div>
    </div>`;

    setTimeout(() => {
        // Oponente simulado con OVR similar
        const opponentOvr = player.hp + Math.floor(Math.random()*6 - 3);
        const opponentCountry = FLAGS_EMOJIS[Math.floor(Math.random() * FLAGS_EMOJIS.length)];
        const opponentNames = ['ProKick_99', 'FutMaster', 'GoalHunter', 'DribleKing', 'FreeKickPro', 'CornerAce'];
        const opponentName  = opponentNames[Math.floor(Math.random() * opponentNames.length)];
        const opponentId    = 'FM-' + String(Math.floor(Math.random()*9000000)+1000000);

        // Simular partido
        const winChance     = 0.5 + (player.hp - opponentOvr) * 0.01;
        const playerGoals   = Math.floor(Math.random() * 4);
        const opponentGoals = Math.random() < winChance
            ? Math.max(0, playerGoals - Math.floor(Math.random()*2))
            : playerGoals + 1 + Math.floor(Math.random()*2);
        const playerWins = playerGoals > opponentGoals;
        const draw = playerGoals === opponentGoals;

        statsPartidas += 2;
        if (statsPartidas > 100) statsPartidas = 100;
        localStorage.setItem('tg_stats_partidas', statsPartidas);
        puntaje += playerWins ? 5 : draw ? 2 : 1;
        actualizarUI();

        const resultColor = playerWins ? 'var(--green)' : draw ? 'var(--gold)' : 'var(--red)';
        const resultText  = playerWins ? '¡VICTORIA! 🏆' : draw ? '¡EMPATE!' : 'DERROTA';

        pvpContent.innerHTML = `
        <div style="margin-bottom:10px">
            <button style="background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.15);border-radius:8px;color:#aaa;padding:7px 14px;cursor:pointer;font-family:'Rajdhani',sans-serif;font-weight:600"
                onclick="resetPVP()">← Volver a Modos</button>
        </div>
        <div class="match-result-box">
            <div style="text-align:center;font-size:0.75em;color:#888;text-transform:uppercase;letter-spacing:2px;margin-bottom:8px">🏟️ FutCancha PVP — ${modo === 'liga' ? 'Liga Mundial' : 'Amistoso'}</div>
            <div style="display:flex;align-items:center;justify-content:space-around;margin:10px 0">
                <div style="text-align:center">
                    <img src="${playerImgSrc(playerIdx)}" style="width:70px;height:88px;object-fit:cover;border-radius:10px;border:2px solid var(--green);display:block;margin:0 auto 6px">
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:0.85em;letter-spacing:1.5px">${player.nombre}</div>
                    <div class="player-id-badge">${playerId}</div>
                    <div style="font-size:0.7em;color:#888;margin-top:2px">OVR ${player.hp}</div>
                </div>
                <div style="text-align:center">
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:4em;color:var(--gold);line-height:1">${playerGoals} - ${opponentGoals}</div>
                    <div style="font-size:0.7em;color:#888;text-transform:uppercase;letter-spacing:2px;margin-top:4px">Final</div>
                </div>
                <div style="text-align:center">
                    <div style="font-size:2.2em;margin-bottom:6px">${opponentCountry}</div>
                    <div style="font-family:'Bebas Neue',sans-serif;font-size:0.85em;letter-spacing:1.5px">${opponentName}</div>
                    <div class="player-id-badge">${opponentId}</div>
                    <div style="font-size:0.7em;color:#888;margin-top:2px">OVR ${opponentOvr}</div>
                </div>
            </div>
            <div class="match-winner-banner" style="border:1px solid ${resultColor};background:rgba(0,0,0,0.3)">
                <h4 style="color:${resultColor}">${resultText}</h4>
                <div style="font-size:0.82em;color:#888;margin-top:6px">+2 pts de estadística · Total: <strong style="color:var(--green)">${statsPartidas}</strong>/100</div>
            </div>
        </div>`;
    }, 3000);
}

function resetPVP() {
    $id('pvp-content').innerHTML = `
    <div class="duel-mode-card" onclick="buscarPartidaPVP('liga')">
        <div class="dm-icon">🏆</div><h3>Liga Mundial</h3>
        <p>Enfrenta a jugadores en línea con la misma categoría de NFT y rango de Overall. Emparejamiento automático por nivel.</p>
        <div class="prizes-row">
            <div class="prize-badge gold-p">🥇 Victoria<br><small>+5 pts estam.</small></div>
            <div class="prize-badge silver-p">🤝 Empate<br><small>+2 pts estam.</small></div>
            <div class="prize-badge bronze-p">❌ Derrota<br><small>+1 pt estam.</small></div>
        </div>
    </div>
    <div class="duel-mode-card" onclick="buscarPartidaPVP('amistoso')">
        <div class="dm-icon">⚽</div><h3>Partido Amistoso</h3>
        <p>Sin penalización. Practica contra otros jugadores. Gana puntos de estadística en cada partido.</p>
        <div class="prizes-row">
            <div class="prize-badge gold-p">🥇 Victoria<br><small>+3 pts estam.</small></div>
            <div class="prize-badge silver-p">🤝 Empate<br><small>+1 pt estam.</small></div>
            <div class="prize-badge bronze-p">❌ Derrota<br><small>+1 pt estam.</small></div>
        </div>
    </div>
    <div class="coming-soon-box">⏳ Emparejamiento en línea — próximamente</div>`;
}

// ── TORNEO ─────────────────────────────────────────────────
async function pagarTorneo() {
    if (!window.tonConnectUI || !tonConnectUI.connected) { mostrarMensaje('⚠️ Conecta tu wallet primero','#ffaa00'); tonConnectUI.openModal(); return; }
    mostrarMensaje('⏳ Procesando inscripción $5 USDT...','#00d4ff');
    const tx = { validUntil: Math.floor(Date.now()/1000)+300, messages: [{ address: WALLET, amount: String(Math.round(1e9)) }] };
    try {
        await tonConnectUI.sendTransaction(tx);
        mostrarMensaje('✅ ¡Inscrito al torneo! Buena suerte 🏆','#ffd700');
        closeModal('modal-torneo');
    } catch(e) { mostrarMensaje('❌ Transacción cancelada','#ff4444'); }
}

// ── RETIRO ──────────────────────────────────────────────────
function retirar() {
    if (!window.tonConnectUI || !tonConnectUI.connected) { mostrarMensaje('⚠️ Conecta tu wallet primero','#ffaa00'); closeModal('modal-retiro'); tonConnectUI.openModal(); return; }
    const cant = parseFloat($id('retiro-cantidad').value) || 0;
    if (cant < 500)   { mostrarMensaje('❌ Mínimo de retiro: 500 💎','#ff4444'); return; }
    if (cant > gemas) { mostrarMensaje(`❌ No tienes ${cant} 💎`,'#ff4444'); return; }
    mostrarMensaje('🔜 Retiro solicitado — Se procesará en 24h','#ffaa00');
    closeModal('modal-retiro');
}

// ── COMPRAR GEMAS ────────────────────────────────────────────
async function comprarGemas(cantidad, tonAmount) {
    if (!window.tonConnectUI || !tonConnectUI.connected) { mostrarMensaje('⚠️ Conecta tu wallet primero','#ffaa00'); closeModal('modal-deposito'); tonConnectUI.openModal(); return; }
    mostrarMensaje('⏳ Procesando pago...','#00d4ff');
    const tx = { validUntil: Math.floor(Date.now()/1000)+300, messages: [{ address: WALLET, amount: String(Math.round(tonAmount*1e9)) }] };
    try {
        await tonConnectUI.sendTransaction(tx);
        gemas += cantidad;
        actualizarUI();
        mostrarMensaje(`✅ +${cantidad} 💎 añadidas`,'#00ff88');
        closeModal('modal-deposito');
    } catch(e) { mostrarMensaje('❌ Transacción cancelada','#ff4444'); }
}

// ── TONCONNECT ───────────────────────────────────────────────
let tonConnectUI;
try {
    tonConnectUI = new TON_CONNECT_UI.TonConnectUI({
        manifestUrl: 'https://turbo-garage-two.vercel.app/tonconnect-manifest.json',
        buttonRootId: 'ton-connect-button'
    });
    tonConnectUI.onStatusChange(w => mostrarMensaje(w ? '✅ Wallet conectada' : 'Wallet desconectada', w ? '#00ff88' : '#888'));
} catch(e) { console.warn('TonConnect no disponible:', e); }

// ── TELEGRAM ─────────────────────────────────────────────────
if (window.Telegram?.WebApp) {
    Telegram.WebApp.ready();
    Telegram.WebApp.expand();
    Telegram.WebApp.setHeaderColor('#060f06');
    Telegram.WebApp.setBackgroundColor('#060f06');
}

// ── INIT ─────────────────────────────────────────────────────
loadDurabilidades();
loadFuelState();
statsPartidas = parseInt(localStorage.getItem('tg_stats_partidas') || '0');
actualizarUI();
buildMarket();
buildLogros();

// Update main player image on load if has players
if (carrosComprados.length > 0) {
    $id('main-car-img').src = playerImgSrc(carrosComprados[0]);
}

</script>
</body>
</html>
